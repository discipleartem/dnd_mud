# dnd_mud

`python version = 3.13`

```
Структура файлов и папок проекта

dnd_mud/
│
├── .github/
│   └── workflows/
│       └── ci-cd.yml
│
├── game/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── game_engine.py
│   │   ├── data/
│   │   │   ├── __init__.py
│   │   │   └── data_loader.py
│   │   └── character/
│   │       ├── __init__.py
│   │       └── character_creator.py
│   │
│   ├── saves/
│   │   ├── __init__.py
│   │   └── .gitkeep
│   │
│   ├── logs/
│   │   ├── __init__.py
│   │   └── .gitkeep
│   │
│   └── data/
│       ├── base/
│       │   ├── races.yaml
│       │   ├── classes.yaml
│       │   └── settings.yaml
│       │
│       ├── schemas/
│       │   ├── race_schema.yaml
│       │   └── class_schema.yaml
│       │
│       └── mods/
│           ├── __init__.py
│           └── .gitkeep
│
├── utils/
│   ├── __init__.py
│   ├── logger.py
│   ├── watchdog_handler.py
│   └── mods_loader.py
│
├── ui/
│   ├── __init__.py
│   ├── welcome_screen.py
│   ├── settings_menu.py
│   ├── window_manager.py
│   └── text_renderer.py
│
├── tests/
│   ├── __init__.py
│   ├── test_config.py
│   ├── test_data_loader.py
│   └── test_window_manager.py
│
├── config/
│   ├── __init__.py
│   ├── config_manager.py
│   └── default_config.yaml
│
├── __init__.py
├── main.py
├── pyproject.toml
├── README.md
├── LICENSE
└── .gitignore
```
## 📋 Описание

Текстовая однопользовательская игры по мотивам Dungeons and Dragons.
Механика игры базируется только на Книге Игрока D&D 5 редакции до 2023 года.
### Особенности

- ✨ Красивый цветной интерфейс с использованием Rich
- 🎮 Создание персонажа с выбором расы и класса
- 💾 Система сохранений в JSON
- 🔧 Полностью настраиваемая через YAML конфиги
- 🎨 Поддержка модификаций (моды)
- 📝 Динамический перенос текста под размер окна
- 🔍 Автоматическое отслеживание изменений файлов (watchdog)
- 📊 Подробное логирование
- 🚀 Кросс-платформенность (Windows, Linux, macOS)

## 🚀 Быстрый старт

### Требования

- Python 3.13 или выше
- pip (менеджер пакетов Python)

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/dnd_mud.git
cd dnd_mud
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -e .
```

### Запуск

```bash
python main.py
```

## 📦 Компиляция в исполняемый файл

### Windows

```bash
pip install pyinstaller
pyinstaller --onefile --console --name DnD_MUD main.py
```

Исполняемый файл будет в папке `dist/DnD_MUD.exe`

### Linux

```bash
pip install pyinstaller
pyinstaller --onefile --console --name dnd_mud main.py
```

Исполняемый файл будет в папке `dist/dnd_mud`

### macOS

```bash
pip install pyinstaller
pyinstaller --onefile --console --name DnD_MUD main.py
```

Исполняемый файл будет в папке `dist/DnD_MUD`

## 🗂️ Структура проекта

```
dnd_mud/
├── game/
│   ├── core/          # Ядро игры
│   ├── saves/         # Сохранения
│   ├── logs/          # Логи
│   └── data/          # Игровые данные
│       ├── base/      # Базовые данные (расы, классы)
│       ├── schemas/   # Схемы валидации
│       └── mods/      # Модификации
├── ui/                # Пользовательский интерфейс
├── utils/             # Утилиты
├── config/            # Конфигурация
├── tests/             # Тесты
└── main.py            # Точка входа
```

## 🎨 Модификации (Моды)

Игра поддерживает модификации для расширения функционала.

### Создание мода

1. Создайте файл в папке `game/data/mods/` (например, `my_mod.yaml`)

2. Структура мода:

```yaml
mod_info:
  name: "Мой Мод"
  version: "1.0.0"
  author: "Ваше Имя"
  description: "Описание мода"

# Добавление новой расы
races:
  elf:
    name: "Эльф"
    description: "Грациозная раса с острым зрением"
    ability_bonuses:
      dexterity: 2
      intelligence: 1
    speed: 30
    languages: ["Common", "Elvish"]
    traits: ["Darkvision", "Keen Senses"]

# Изменение существующих классов
classes:
  wizard:
    additional_spells:
      - "Fireball"
      - "Lightning Bolt"
```

### Включение/отключение модов

Моды можно включить/отключить в настройках игры или в `config/default_config.yaml`:

```yaml
mods:
  enabled: true  # true - включены, false - выключены
```

### Порядок загрузки

Моды загружаются в алфавитном порядке. Более поздние моды могут переопределять изменения предыдущих.

## ⚙️ Настройки

Настройки игры находятся в `config/default_config.yaml` и `config/user_config.yaml`.

### Основные настройки

```yaml
# Отображение
display:
  colored_text: true      # Цветной текст
  min_width: 80          # Минимальная ширина
  max_width: 120         # Максимальная ширина

# Игра
game:
  language: ru           # Язык (ru, en)
  difficulty: easy       # Сложность (easy, hard_core)
  autosave: true         # Автосохранение
  autosave_interval: 300 # Интервал автосохранения (сек)

# Модификации
mods:
  enabled: true          # Включить моды
  auto_reload: true      # Автоперезагрузка при изменении

# Логирование
logging:
  enabled: true
  level: "INFO"          # DEBUG, INFO, WARNING, ERROR
```

## 🧪 Тестирование

Запуск тестов:

```bash
pytest
```

С покрытием:

```bash
pytest --cov=game --cov=utils --cov=ui --cov=config
```

## 📝 Разработка

### Установка dev-зависимостей

```bash
pip install -e ".[dev]"
```

### Форматирование кода

```bash
black .
```

### Проверка стиля

```bash
flake8
```

### Проверка типов

```bash
mypy .
```

## 🐛 Решение проблем

### Проблема: ModuleNotFoundError

**Решение:**
```bash
# Убедитесь что установили проект
pip install -e .

# Или добавьте PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:/path/to/dnd_mud"  # Linux/macOS
set PYTHONPATH=%PYTHONPATH%;C:\path\to\dnd_mud     # Windows
```

### Проблема: Кириллица не отображается

**Решение:**
```bash
# Windows: Установите кодировку консоли
chcp 65001

# Или в настройках игры отключите цветной текст
```

### Проблема: PyInstaller не находит модули

**Решение:**
```bash
# Явно укажите hidden imports
pyinstaller --onefile --console \
  --hidden-import=yaml \
  --hidden-import=rich \
  --hidden-import=watchdog \
  main.py
```

## 📦 Дистрибуция

### Создание ZIP архива для Windows:

```bash
# После сборки PyInstaller
cd dist
mkdir DnD_MUD_Portable
copy DnD_MUD.exe DnD_MUD_Portable\
xcopy /E /I ..\game\data DnD_MUD_Portable\game\data
xcopy /E /I ..\config DnD_MUD_Portable\config
copy ..\README.md DnD_MUD_Portable\

# Создайте ZIP
# Используйте 7zip или встроенный архиватор Windows
```

### Создание .deb пакета для Ubuntu:

```bash
# Установите необходимые инструменты
sudo apt-get install debhelper

# Создайте структуру пакета
mkdir -p dnd-mud/DEBIAN
mkdir -p dnd-mud/usr/local/bin
mkdir -p dnd-mud/usr/share/dnd-mud

# Скопируйте файлы
cp dist/dnd_mud dnd-mud/usr/local/bin/
cp -r game/data dnd-mud/usr/share/dnd-mud/
cp -r config dnd-mud/usr/share/dnd-mud/

# Создайте control файл
cat > dnd-mud/DEBIAN/control << EOF
Package: dnd-mud
Version: 0.1.0
Section: games
Priority: optional
Architecture: amd64
Maintainer: Your Name <your.email@example.com>
Description: D&D MUD Text Adventure Game
 Текстовая ролевая игра по мотивам Dungeons & Dragons 5e
EOF

# Соберите пакет
dpkg-deb --build dnd-mud
```

## 🔄 CI/CD с GitHub Actions

После push в репозиторий GitHub Actions автоматически:
1. Запустит тесты на Ubuntu, Windows, macOS
2. Проверит стиль кода
3. Соберет исполняемые файлы для всех платформ
4. При создании релиза прикрепит собранные файлы

## 📚 Дополнительные ресурсы

- [Python документация](https://docs.python.org/3/)
- [Rich библиотека](https://rich.readthedocs.io/)
- [PyInstaller документация](https://pyinstaller.org/)
- [Pytest документация](https://docs.pytest.org/)

---

## 📄 Лицензия

MIT License. См. файл [LICENSE](LICENSE) для деталей.

## 🤝 Вклад в проект

Приветствуются Pull Request'ы!  
Для крупных изменений сначала откройте Issue для обсуждения.

## 📧 Контакты

- GitHub Issues: [Создать Issue](https://github.com/yourusername/dnd_mud/issues)
- Telegram: [@disciple_artem](https://t.me/disciple_artem)
- Discord: [Discord](https://discord.gg/disciple_artem)

## 🙏 Благодарности

- Wizards of the Coast за D&D 5e
- Сообщество Python за отличные библиотеки
- Всем контрибьюторам проекта

---

**Приятной игры! 🎲**