# DnD MUD (переписывание с 0)

Текстовая однопользовательская MUD-игра на Python 3.13+ по мотивам D&D 5e (2023).

## Архитектура

Проект построен по принципам **Чистой Архитектуры** (Clean Architecture) Роберта Мартина с четким разделением на 4 слоя:

### 🏛️ Domain Layer (Enterprise Business Rules)
- **entities/** - бизнес-сущности (Character, Race, Class)
- **value_objects/** - объекты-значения (Dice, Attributes, Skills)
- **services/** - доменные сервисы (CharacterGeneration)
- **repositories/** - интерфейсы репозиториев
- **interfaces/** - интерфейсы для внешних систем

### 🎯 Application Layer (Application Business Rules)
- **use_cases/** - сценарии использования (CreateCharacter, LoadGame)
- **dto/** - объекты передачи данных
- **interfaces/** - интерфейсы для адаптеров

### 🔌 Interface Adapters Layer
- **repositories/** - реализации репозиториев (YAML, JSON)
- **presenters/** - презентеры (форматирование данных для UI)
- **controllers/** - контроллеры (обработка ввода)
- **gateways/** - шлюзы к внешним системам

### 🛠️ Infrastructure Layer (Frameworks & Drivers)
- **ui/** - пользовательский интерфейс (консольный)
- **persistence/** - конкретные реализации хранения
- **localization/** - система локализации
- **config/** - управление конфигурацией
- **mods/** - поддержка модов

## Быстрый старт

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"

dnd-mud
```

## Структура проекта

```
src/
├── domain/                    # Enterprise Business Rules
│   ├── entities/             # Character, Race, Class
│   ├── value_objects/        # Dice, Attributes, Skills
│   ├── services/             # CharacterGeneration
│   ├── repositories/         # Интерфейсы репозиториев
│   └── interfaces/          # Интерфейсы для внешних систем
├── application/               # Application Business Rules
│   ├── use_cases/           # CreateCharacter, LoadGame
│   ├── dto/                 # Data Transfer Objects
│   └── interfaces/          # Интерфейсы для адаптеров
├── adapters/                # Interface Adapters
│   ├── repositories/        # Реализации репозиториев
│   ├── presenters/          # Презентеры
│   ├── controllers/         # Контроллеры
│   └── gateways/           # Шлюзы к внешним системам
└── infrastructure/          # Frameworks & Drivers
    ├── ui/                 # Консольный интерфейс
    ├── persistence/        # Сохранение/загрузка
    ├── localization/       # Локализация
    ├── config/            # Конфигурация
    └── mods/              # Моды
data/                          # Игровые данные и контент
├── yaml/                   # YAML конфигурации
│   ├── attributes/         # Характеристики, классы, расы
│   ├── dice/              # Типы кубиков
│   ├── items/             # Предметы
│   ├── localization/      # Переводы
│   └── monsters/          # Монстры
├── saves/                  # Сохранения персонажей
└── mods/                   # Пользовательские моды
tests/                         # Тесты
├── test_dice.py           # Тесты системы кубиков
└── test_character.py      # Тесты персонажей
```

## Принципы архитектуры

### Dependency Rule
**Зависимости могут направляться только внутрь**:
- Domain → ничего не зависит
- Application → Domain
- Adapters → Application + Domain
- Infrastructure → Adapters + Application + Domain

### SOLID Principles
- **Single Responsibility** - каждый класс имеет одну причину для изменения
- **Open/Closed** - открыт для расширения, закрыт для изменения
- **Liskov Substitution** - подтипы могут заменять базовые типы
- **Interface Segregation** - множество интерфейсов лучше одного общего
- **Dependency Inversion** - зависимость от абстракций, а не реализаций

## Разработка

### Тестирование
```bash
# Запустить все тесты
pytest

# Запустить конкретный тест
pytest tests/test_dice.py

# С покрытием
pytest --cov=src tests/
```

### Качество кода
```bash
# Форматирование
black src/

# Линтинг
ruff check src/

# Типизация
mypy src/
```

## Лицензия

MIT License

