---
description: dnd_mud — максимально простой Python без лишних абстракций
globs: "core/**,ui/**,main.py,tests/**"
alwaysApply: false
---

# dnd_mud — простой Python

См. [`00-global.mdc`](~/.cursor/rules/00-global.mdc) (KISS/DRY/YAGNI) и `python-standards.mdc`.  
`python-architecture.mdc` — только если паттерн реально упрощает код в этом MUD.

## Предпочитать

- Функции вместо классов без состояния
- `dataclass` для моделей данных (`core/models.py`)
- Guard clauses и ранний `return`/`continue` — max 1–2 уровня вложенности
- Явные `if` / `match` вместо динамических диспетчеров
- Простые comprehensions (один цикл, одно условие)

## Избегать

- Фабрики, Strategy, Repository для 1–2 вариантов
- Лямбды вместо именованных функций
- Вложенные comprehensions и цепочки тернарников
- Словари с `lambda` как обработчики меню
- Хелперы, используемые один раз

## Пример

```python
# ✅ явные if/return
def handle_menu_choice(choice: int, loc, settings) -> tuple[bool, object]:
    if choice == 2:
        return False, settings
    if choice == 1:
        return _handle_settings(loc, settings)
    return True, settings

# ❌ lambda-dispatch
menu_handlers = {1: lambda l, s: _handle_settings(l, s), ...}
```

## Тесты

См. `dnd-mud-tests.mdc`.

## Чек-лист перед коммитом

- [ ] Можно ли проще? (KISS/YAGNI)
- [ ] Нужен ли класс, или хватит функции?
- [ ] Будет ли код понятен через месяц?
- [ ] Нужен ли тест? (см. `dnd-mud-tests.mdc`)
