---
description: dnd_mud — console MUD, индекс локальных правил, команды
alwaysApply: true
---

# dnd_mud project

**Stack:** Python **3.12** (основной язык), console MUD, Colorama, file-based storage, pytest.

## Стиль кода

Максимально простой Python — без преждевременных абстракций и паттернов.
Подробности: `dnd-mud-python-simple.mdc`.

## Данные

Пути, форматы YAML/JSON, моды — **единственный источник:** `dnd-mud-data.mdc`.
Чтение/запись только через модули `core/` — не из `ui/`.

## Верификация

Консольное приложение — **не web**. Правило `dnd-mud-verify.mdc` **приоритетнее** глобальных browser-правил:

- `make test` + console smoke (`python main.py`)
- Browser automation **не использовать**

## Commands

```bash
make install    # venv + pip install -e ".[dev]"
make test       # pytest
make check      # ruff --fix + black + mypy
python main.py  # smoke: пройти затронутое меню
```

## Docs

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — слои UI → core → database
- [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — установка, моды, git
- [docs/MUD_PRD.md](docs/MUD_PRD.md) — требования и статус Pre-Alpha

## Local rules (index)

| Файл | Globs | Содержание |
|------|-------|------------|
| `dnd-mud-python-simple.mdc` | `core/**`, `ui/**`, `main.py`, `tests/**` | простой Python без лишних абстракций |
| `dnd-mud-core.mdc` | `core/**`, `ui/**`, `main.py` | слои, локализация, механика |
| `dnd-mud-data.mdc` | `database/**`, `mods/**`, `**/*.json` | YAML (данные), JSON (сейвы/конфиги) |
| `dnd-mud-tests.mdc` | `tests/**` | pytest — простые, необходимые тесты |
| `dnd-mud-git.mdc` | always | scope и dnd_mud-артефакты (extends `01-operations.mdc`) |
| `dnd-mud-verify.mdc` | always | верификация (не browser) |

Глобальные правила: `~/.cursor/rules/` (см. `AGENTS.md`, `~/.cursor/rules/00-global.mdc`).
