---
description: dnd_mud verification, git scope, finish checklist — console MUD, not browser
alwaysApply: true
---

# dnd_mud verification

Console MUD — browser forbidden. Verify: `make test` + console smoke.

## Git scope

См. [`01-operations.mdc`](~/.cursor/rules/01-operations.mdc). Не коммитить: `.coverage`, `saves/`.

Обязательно commit/push — задачи с code/data (не docs-only).

## Before finishing

1. **Git** — `01-operations.mdc` (старт/sync/push); task-ветка; clean tree
2. **Verify** — `make test` / `make check` / `python main.py` — см. [`00-project.mdc`](00-project.mdc) §Commands

Implement once at end; skip runtime for docs-only.
