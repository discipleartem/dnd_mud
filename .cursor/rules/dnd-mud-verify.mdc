---
description: dnd_mud verification, git scope, finish checklist — console MUD, not browser
alwaysApply: true
---

# dnd_mud verification

Git-flow и Cursor modes — [`00-global.mdc`](~/.cursor/rules/00-global.mdc) §Cursor modes, [`01-operations.mdc`](~/.cursor/rules/01-operations.mdc) §Task cycle.

Console MUD — browser forbidden.

## Git scope

Не коммитить: `.coverage`, `saves/`.

## Verify

Переопределяет `ironbee-devtools-use.mdc` и `browser-automation.mdc`.

```bash
make test       # обязательно
make check      # при изменениях типов/импортов
python main.py  # smoke при затронутом UI
```

## Before finishing

- **Plan-сессия:** git и push — global `01-operations`; verify — команды выше
- **Agent напрямую:** verify при затронутом коде/UI

Implement once at end; skip runtime for docs-only.
