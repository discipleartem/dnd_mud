---
description: dnd_mud — overrides, verify/review policy (console MUD)
alwaysApply: true
---

# dnd_mud verification

Console MUD — browser forbidden. Переопределяет `ironbee-devtools-use.mdc` и `browser-automation.mdc`.

Оркестрация: [`AGENTS.md`](../AGENTS.md). Git-flow: [`dnd-mud-git.mdc`](dnd-mud-git.mdc).

## Переопределения global

| Global | dnd_mud |
|--------|---------|
| Browser / IronBee verify | **`make test`**, smoke `python main.py` |

Git-старт, push, parallel dev — [`dnd-mud-git.mdc`](dnd-mud-git.mdc).

## Процедуры (канон в skills)

| Шаг | Skill |
|-----|-------|
| Docs после реализации | [`dnd-mud-docs-after-task`](../skills/dnd-mud-docs-after-task/SKILL.md) |
| Verify (runtime) | [`dnd-mud-verify`](../skills/dnd-mud-verify/SKILL.md) |
| Review (локальный subagent, до push/PR/merge) | [`dnd-mud-review`](../skills/dnd-mud-review/SKILL.md) |
| Fix plan (после review, Major+ или по запросу) | [`dnd-mud-fix-plan`](../skills/dnd-mud-fix-plan/SKILL.md) |
| Release `dev` → `main` | [`dnd-mud-release`](../skills/dnd-mud-release/SKILL.md) |

Verify ≠ review. Оба — **один раз** в конце task-ветки, после docs. Runtime не нужен для docs-only / rules-only.

**Review:** только локальный Cursor subagent (`dnd-mud-review`). **GitHub PR Bugbot не используется** — не включать в Cursor dashboard, `.cursor/BUGBOT.md` не вести.
