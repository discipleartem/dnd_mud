# Чистая Архитектура в DnD MUD

## Обзор

Проект DnD MUD реализован с использованием **Чистой Архитектуры** (Clean Architecture) Роберта Мартина. Этот подход обеспечивает четкое разделение ответственности, тестируемость и независимость от внешних фреймворков.

## 4 Слоя Архитектуры

### 🏛️ Domain Layer (Enterprise Rules)

**Самый внутренний слой** - содержит бизнес-правила, которые не зависят от внешних систем.

```
src/domain/
├── entities/          # Бизнес-сущности
│   ├── character.py   # Персонаж D&D
│   ├── race.py       # Раса персонажа
│   ├── class_.py     # Класс персонажа
│   └── ...
├── value_objects/    # Объекты-значения
│   ├── dice.py       # Система кубиков
│   ├── attributes.py # Характеристики
│   ├── skills.py     # Навыки
│   └── saving_throws.py # Спасброски
├── services/         # Доменные сервисы
│   └── character_generation.py # Генерация персонажей
├── repositories/     # Интерфейсы репозиториев
└── interfaces/      # Интерфейсы для внешних систем
    └── localization.py # Интерфейс локализации
```

**Принципы:**
- Никаких зависимостей от внешних слоев
- Только бизнес-логика D&D 5e
- Максимальная изоляция и тестируемость

### 🎯 Application Layer (Use Cases)

**Слой приложений** - оркестрация доменных объектов для реализации сценариев использования.

```
src/application/
├── use_cases/        # Сценарии использования
│   ├── create_character.py  # Создание персонажа
│   ├── load_game.py        # Загрузка игры
│   └── save_game.py       # Сохранение игры
├── dto/              # Data Transfer Objects
│   └── character_dto.py    # DTO для персонажа
└── interfaces/       # Интерфейсы для адаптеров
```

**Принципы:**
- Зависит только от Domain слоя
- Реализует конкретные бизнес-сценарии
- Не содержит внешних зависимостей

### 🔌 Interface Adapters Layer

**Адаптеры интерфейсов** - преобразование данных между форматами разных слоев.

```
src/adapters/
├── repositories/     # Реализации репозиториев
│   └── character_repository.py # Хранение персонажей в YAML
├── presenters/       # Презентеры
├── controllers/      # Контроллеры
└── gateways/        # Шлюзы к внешним системам
    ├── localization/   # Адаптеры локализации
    └── localization_adapter.py
```

**Принципы:**
- Преобразует данные из формата Domain в формат для UI
- Реализует интерфейсы из Application слоя
- Изолирует внешние системы

### 🛠️ Infrastructure Layer (Frameworks & Drivers)

**Самый внешний слой** - конкретные реализации фреймворков, UI, баз данных.

```
src/infrastructure/
├── ui/               # Пользовательский интерфейс
│   ├── menus/        # Меню игры
│   ├── renderer.py   # Рендеринг консоли
│   └── input_handler.py # Обработка ввода
├── persistence/      # Сохранение данных
├── localization/     # Система локализации
├── config/          # Управление конфигурацией
│   └── config_manager.py
└── mods/            # Поддержка модов
```

**Принципы:**
- Содержит все внешние зависимости
- Легко заменяемый (UI, БД, фреймворки)
- Не влияет на бизнес-логику

## Dependency Rule

**Главное правило Чистой Архитектуры:**

> **Зависимости могут направляться только внутрь**

```
Infrastructure → Adapters → Application → Domain
     ↑              ↑           ↑          ↑
   Внешний       Преобраз.   Бизнес-    Ядро
  мир           данных      логика     правил
```

### Что это значит на практике:

1. **Domain** не зависит ни от чего
2. **Application** зависит только от Domain
3. **Adapters** зависит от Application и Domain
4. **Infrastructure** зависит от всех вышестоящих слоев

## Примеры в коде

### Domain Layer - Чистая бизнес-логика

```python
# src/domain/entities/character.py
class Character:
    def __init__(self, name: str, race: Race, character_class: CharacterClass):
        self.name = name
        self.race = race
        self.character_class = character_class
        # Только бизнес-логика D&D!
    
    def get_ability_modifier(self, value: int) -> int:
        """Расчет модификатора характеристики по правилам D&D."""
        return (value - 10) // 2
```

### Application Layer - Use Case

```python
# src/application/use_cases/create_character.py
class CreateCharacterUseCase:
    def __init__(self, character_repo: CharacterRepository):
        self.character_repo = character_repo
    
    def execute(self, request: CreateCharacterRequest) -> Character:
        # Создаем персонажа через доменные сервисы
        character = CharacterFactory.create(
            name=request.name,
            race=request.race,
            character_class=request.character_class
        )
        
        # Сохраняем через репозиторий
        return self.character_repo.save(character)
```

### Adapters Layer - Реализация репозитория

```python
# src/adapters/repositories/character_repository.py
class CharacterRepository:
    def save(self, character: Character) -> Character:
        """Сохранение персонажа в YAML файл."""
        save_data = CharacterSaveData.from_character(character)
        file_path = self._get_save_path(character.name)
        
        with open(file_path, 'w') as f:
            yaml.dump(save_data.to_dict(), f)
        
        return character
```

### Infrastructure Layer - UI

```python
# src/infrastructure/ui/menus/character_creation.py
class CharacterCreationController:
    def create_character(self) -> Optional[Character]:
        # Используем use case из Application слоя
        use_case = CreateCharacterUseCase(self.character_repo)
        
        # Собираем данные от пользователя
        request = self._collect_user_input()
        
        # Выполняем бизнес-логику
        return use_case.execute(request)
```

## Преимущества подхода

### 🧪 **Тестируемость**
- Domain слой можно тестировать изолированно
- Use Cases тестируются с моками репозиториев
- UI не влияет на тестирование бизнес-логики

### 🔄 **Гибкость**
- Легко заменить консольный UI на веб-интерфейс
- Можно поменять YAML на PostgreSQL без изменения бизнес-логики
- Поддержка различных форматов конфигурации

### 📈 **Масштабируемость**
- Новые функции добавляются в соответствующие слои
- Четкие границы предотвращают "спагетти-код"
- Командная разработка с разделением по слоям

### 🛡️ **Надежность**
- Бизнес-правила защищены от изменений в UI
- Внешние зависимости изолированы
- Легко откатывать изменения в конкретных слоях

## Паттерны проектирования

Используемые паттерны в рамках Чистой Архитектуры:

### Repository Pattern
```python
# Интерфейс в Domain
class CharacterRepository(ABC):
    @abstractmethod
    def save(self, character: Character) -> Character: pass
    
    @abstractmethod
    def load(self, name: str) -> Optional[Character]: pass

# Реализация в Adapters
class YamlCharacterRepository(CharacterRepository):
    def save(self, character: Character) -> Character: ...
```

### Factory Pattern
```python
# Domain слой
class CharacterFactory:
    @staticmethod
    def create(name: str, race: str, class_: str) -> Character:
        return Character(
            name=name,
            race=RaceFactory.create_race(race),
            character_class=ClassFactory.create_class(class_)
        )
```

### Adapter Pattern
```python
# Адаптер для локализации
class LocalizationAdapter(LocalizationInterface):
    def __init__(self, legacy_loader):
        self._loader = legacy_loader
    
    def get_text(self, key: str, **kwargs) -> str:
        return self._loader.get_text(key, **kwargs)
```

## Рекомендации по разработке

### 🎯 **При добавлении новой функции:**

1. **Domain Layer** - Добавить бизнес-сущности/правила
2. **Application Layer** - Создать use case для сценария
3. **Adapters Layer** - Реализовать необходимые адаптеры
4. **Infrastructure Layer** - Добавить UI/конфигурацию

### 📝 **При написании кода:**

- Следуйте Dependency Rule
- Используйте интерфейсы для зависимостей
- Тестируйте каждый слой изолированно
- Документируйте бизнес-правила в Domain

### 🚫 **Чего избегать:**

- Циклических зависимостей между слоями
- Прямых вызовов из Infrastructure в Domain
- Бизнес-логики в UI слое
- Зависимостей от конкретных фреймворков в Domain

Эта архитектура обеспечивает долгосрочную поддерживаемость и масштабируемость проекта DnD MUD.
