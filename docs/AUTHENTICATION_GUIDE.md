# Руководство по аутентификации cursor-agent

## 🎯 Быстрый старт

Для работы MCP агентов требуется аутентификация `cursor-agent`. Есть два способа:

## Способ 1: Интерактивный login (рекомендуется)

### Шаг 1: Выполните команду в терминале

```bash
export PATH="$HOME/.local/bin:$PATH"
cursor-agent login
```

### Шаг 2: Аутентификация в браузере

1. Команда откроет браузер (или покажет ссылку)
2. Войдите в свой аккаунт Cursor
3. Подтвердите доступ для CLI
4. После успешной аутентификации вернитесь в терминал

### Шаг 3: Проверка

```bash
cursor-agent status
# Должно показать: "Logged in" или информацию о вашем аккаунте
```

## Способ 2: Использование API ключа

Если вы предпочитаете использовать API ключ вместо интерактивного login:

### Шаг 1: Получите API ключ

1. Откройте https://cursor.com/settings
2. Найдите раздел "API Keys" или "Developer Settings"
3. Создайте новый API ключ
4. Скопируйте ключ (он показывается только один раз!)

### Шаг 2: Добавьте ключ в конфигурацию MCP

Отредактируйте `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "sub-agents": {
      "env": {
        "CURSOR_API_KEY": "ваш_api_ключ_здесь",
        "PATH": "/home/tomas/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
      }
    }
  }
}
```

### Шаг 3: Перезапустите Cursor IDE

После добавления API ключа перезапустите Cursor IDE, чтобы MCP сервер подхватил изменения.

## ✅ Проверка после аутентификации

После выполнения любого из способов проверьте:

```bash
# Проверка статуса
export PATH="$HOME/.local/bin:$PATH"
cursor-agent status

# Должно показать информацию о вашем аккаунте
```

## 🧪 Тестирование запуска агента

После успешной аутентификации протестируйте запуск агента:

```python
# В Cursor IDE через MCP
mcp_sub-agents_run_agent(
    agent="code-reviewer",
    prompt="Проверь код файла /path/to/file.py"
)
```

## 🔍 Устранение проблем

### Ошибка: "Authentication required"

**Решение:**
- Убедитесь, что выполнили `cursor-agent login` ИЛИ добавили `CURSOR_API_KEY` в конфигурацию
- Перезапустите Cursor IDE после изменения конфигурации

### Ошибка: "cursor-agent: command not found"

**Решение:**
```bash
export PATH="$HOME/.local/bin:$PATH"
# Или добавьте в ~/.bashrc:
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### MCP сервер не видит cursor-agent

**Решение:**
- Убедитесь, что PATH добавлен в `~/.cursor/mcp.json` в секции `env`
- Перезапустите Cursor IDE

## 📋 Чек-лист

- [ ] `cursor-agent` установлен и доступен в PATH
- [ ] Выполнен `cursor-agent login` ИЛИ добавлен `CURSOR_API_KEY`
- [ ] `cursor-agent status` показывает успешную аутентификацию
- [ ] PATH добавлен в `~/.cursor/mcp.json`
- [ ] Cursor IDE перезапущен после изменений
- [ ] Тестовый запуск агента работает

## 🚀 После успешной аутентификации

Вы сможете использовать все MCP агенты:
- `code-reviewer` - ревью кода
- `test-writer` - написание тестов
- `security-checker` - проверка безопасности
- `doc-writer` - документация
- `architect` - архитектурные решения

