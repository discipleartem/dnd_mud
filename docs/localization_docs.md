# 🌍 Система локализации DnD MUD Game

## Обзор

Система локализации поддерживает множественные источники переводов с приоритизацией:
- **Приключения** (priority 200) - наивысший приоритет
- **Моды** (priority 100) - средний приоритет  
- **Базовая локализация** (priority 0) - базовый уровень

Это позволяет приключениям и модам переопределять базовые переводы.

---

## Архитектура

### Паттерны проектирования

1. **Singleton** - единственный экземпляр `LocalizationManager`
2. **Chain of Responsibility** - поиск перевода в цепочке источников
3. **Strategy** - разные стратегии загрузки (база, моды, приключения)

### Принципы

- **DRY** - единая система для всех типов локализации
- **SRP** - менеджер отвечает только за локализацию
- **OCP** - легко добавлять новые источники

---

## Базовая локализация

### Расположение
`data/yaml/localization.yaml`

### Структура

```yaml
ru:
  menu:
    title: "Главное меню"
    continue: "Продолжить"
    new_game: "Новая игра"
    
  menu_desc:
    continue: "Продолжить последнюю игру"
    new_game: "Начать новое приключение"
    
  messages:
    welcome: "Добро пожаловать!"
    
  combat:
    hit: "Вы нанесли {damage} урона существу {target}"
    miss: "Вы промахнулись по {target}"

en:
  menu:
    title: "Main Menu"
    continue: "Continue"
    new_game: "New Game"
    
  # ... английские переводы
```

---

## Использование в коде

### Импорт

```python
from src.data.localization import loc, localization_manager
```

### Простое получение строки

```python
# Получение перевода
text = loc("menu.continue")
# Результат: "Продолжить" (если язык = ru)

# С fallback значением
text = loc("unknown.key", default="Default text")
# Результат: "Default text" (если ключ не найден)
```

### Форматирование строк

```python
# С параметрами
message = loc("combat.hit", target="Гоблин", damage=5)
# Результат: "Вы нанесли 5 урона существу Гоблин"

# Параметры автоматически подставляются в {placeholders}
```

### Смена языка

```python
# Установка языка
localization_manager.set_language("en")

# Получение текущего языка
current = localization_manager.get_language()  # "en"
```

### Проверка наличия ключа

```python
if localization_manager.has_key("menu.continue"):
    text = loc("menu.continue")
```

---

## Локализация модов

### Требования

**КАЖДЫЙ МОД ОБЯЗАН ИМЕТЬ `localization.yaml`!**

### Структура мода

```
data/mods/my_awesome_mod/
├── mod.yaml              # Метаданные
├── localization.yaml     # ОБЯЗАТЕЛЬНО!
├── mechanics.yaml        # Опционально
└── README.md
```

### Метаданные (mod.yaml)

```yaml
name: "My Awesome Mod"
name_localized: "mod.my_mod.name"  # Ссылка на локализацию
version: "1.0.0"
author: "Your Name"
description_localized: "mod.my_mod.description"
compatible_version: "0.1.0"

adds:
  items: true
  mechanics: true
```

### Локализация (localization.yaml)

```yaml
ru:
  mod:
    my_mod:
      name: "Мой Потрясающий Мод"
      description: "Добавляет новые механики"
      
  # Новые механики
  mechanics:
    new_ability:
      name: "Новая способность"
      description: "Описание способности"
      
  # Новые предметы
  items:
    magic_sword:
      name: "Волшебный меч"
      description: "Меч с магическими свойствами"
      
  # Сообщения
  messages:
    ability_used: "Вы использовали {ability}!"

en:
  mod:
    my_mod:
      name: "My Awesome Mod"
      description: "Adds new mechanics"
      
  # ... английская локализация
```

### Загрузка мода

```python
from pathlib import Path
from src.data.localization import localization_manager

# Загрузка локализации мода
mod_path = Path("data/mods/my_awesome_mod")
localization_manager.load_mod_localization("my_awesome_mod", mod_path)

# Теперь можно использовать ключи мода
name = loc("mod.my_mod.name")
# "Мой Потрясающий Мод"

item_name = loc("items.magic_sword.name")
# "Волшебный меч"
```

### Выгрузка мода

```python
localization_manager.unload_mod("my_awesome_mod")
```

---

## Локализация приключений

### Требования

**КАЖДОЕ ПРИКЛЮЧЕНИЕ ОБЯЗАНО ИМЕТЬ `localization.yaml`!**

### Структура приключения

```
data/adventures/lost_mine/
├── adventure.yaml        # Метаданные
├── localization.yaml     # ОБЯЗАТЕЛЬНО!
├── mechanics.yaml        # Опционально (изменения правил)
├── quests/
│   └── main_quest.yaml
├── locations/
│   └── phandalin.yaml
└── npcs/
    └── sildar.yaml
```

### Метаданные (adventure.yaml)

```yaml
name: "Lost Mine of Phandelver"
name_localized: "adventure.lost_mine.name"
version: "1.0.0"
description_localized: "adventure.lost_mine.description"
compatible_version: "0.1.0"

requires:
  mods: []  # Зависимости от модов
  min_level: 1
  max_level: 5

content:
  locations: true
  quests: true
  npcs: true
```

### Локализация (localization.yaml)

```yaml
ru:
  adventure:
    lost_mine:
      name: "Затерянная шахта Фанделвера"
      description: "Классическое приключение"
      intro: |
        Длинный текст вступления...
        
  # Локации
  locations:
    phandalin:
      name: "Фандалин"
      description: "Небольшой пограничный городок"
      first_visit: "Вы впервые видите Фандалин..."
      
  # NPC
  npcs:
    sildar:
      name: "Силдар Холлвинтер"
      title: "Член Альянса Лордов"
      greeting: "Благодарю за спасение!"
      
  # Квесты
  quests:
    main_quest:
      name: "Найти Гандрена"
      description: "Разыскать пропавшего дварфа"
      objective: "Найти Гандрена Искателя Скал"
      complete: "Вы нашли Гандрена!"
      
  # Диалоги
  dialogues:
    sildar_first:
      text: "Кто вы? Спасители, надеюсь..."
      option1: "Мы здесь чтобы помочь"
      option2: "Расскажите что случилось"

en:
  adventure:
    lost_mine:
      name: "Lost Mine of Phandelver"
      # ... английская локализация
```

### Система диалогов

Упрощённая система для легкой локализации:

```yaml
dialogues:
  dialogue_id:
    text: "Основной текст диалога"
    options:
      - key: "option1"
        text: "Текст выбора 1"
        next: "next_dialogue"
      - key: "option2"
        text: "Текст выбора 2"
        next: "another_dialogue"
```

В коде:

```python
# Получение текста диалога
text = loc("dialogues.sildar_first.text")

# Получение опций
opt1 = loc("dialogues.sildar_first.option1")
opt2 = loc("dialogues.sildar_first.option2")
```

### Загрузка приключения

```python
from pathlib import Path
from src.data.localization import localization_manager

# Загрузка локализации приключения
adventure_path = Path("data/adventures/lost_mine")
localization_manager.load_adventure_localization("lost_mine", adventure_path)

# Теперь можно использовать ключи приключения
name = loc("adventure.lost_mine.name")
# "Затерянная шахта Фанделвера"

location = loc("locations.phandalin.name")
# "Фандалин"
```

---

## Приоритеты и переопределение

### Порядок поиска

При вызове `loc("some.key")` поиск идёт в порядке приоритета:

1. **Приключения** (200) - самый высокий
2. **Моды** (100)
3. **Базовая локализация** (0) - самый низкий

### Пример переопределения

**Базовая локализация:**
```yaml
ru:
  combat:
    critical_hit: "Критическое попадание!"
```

**Мод (advanced_combat):**
```yaml
ru:
  combat:
    critical_hit: "⚔️ КРИТИЧЕСКИЙ УДАР! ⚔️"
```

После загрузки мода:
```python
text = loc("combat.critical_hit")
# "⚔️ КРИТИЧЕСКИЙ УДАР! ⚔️"  (из мода, не из базы)
```

---

## Кэширование

Система автоматически кэширует переводы для производительности:

- Кэш очищается при смене языка
- Кэш очищается при загрузке/выгрузке модов
- Кэш НЕ используется для строк с параметрами

---

## Отладка

### Получение информации

```python
# Список загруженных источников
sources = localization_manager.get_loaded_sources()

# Отладочная информация
debug = localization_manager.debug_info()
print(debug)
```

Вывод:
```
Текущий язык: ru
Загружено источников: 3
Размер кэша: 42

Источники (по приоритету):
  - adventure_lost_mine (тип: adventure, приоритет: 200, языки: ['ru', 'en'])
  - mod_advanced_combat (тип: mod, приоритет: 100, языки: ['ru', 'en'])
  - base (тип: base, приоритет: 0, языки: ['ru', 'en'])
```

### Получение всех ключей

```python
# Все ключи
all_keys = localization_manager.get_all_keys()

# Ключи с префиксом
menu_keys = localization_manager.get_all_keys(prefix="menu")
# ['menu.title', 'menu.continue', 'menu.new_game', ...]
```

---

## Лучшие практики

### ✅ DO (Делайте)

1. **Всегда создавайте локализацию для модов и приключений**
   ```yaml
   # ✅ Хорошо
   localization.yaml - присутствует
   ```

2. **Используйте понятные ключи**
   ```python
   # ✅ Хорошо
   loc("combat.critical_hit")
   loc("npcs.sildar.greeting")
   ```

3. **Используйте форматирование для параметров**
   ```yaml
   # ✅ Хорошо
   damage_dealt: "Вы нанесли {damage} урона {target}"
   ```

4. **Группируйте связанные ключи**
   ```yaml
   # ✅ Хорошо
   combat:
     hit: "..."
     miss: "..."
     critical: "..."
   ```

### ❌ DON'T (Не делайте)

1. **Не создавайте моды без локализации**
   ```
   # ❌ Плохо
   my_mod/
     mod.yaml
     # localization.yaml отсутствует!
   ```

2. **Не хардкодите строки**
   ```python
   # ❌ Плохо
   print("Критическое попадание!")
   
   # ✅ Хорошо
   print(loc("combat.critical_hit"))
   ```

3. **Не используйте слишком длинные ключи**
   ```python
   # ❌ Плохо
   loc("this.is.a.very.long.key.that.is.hard.to.read")
   
   # ✅ Хорошо
   loc("combat.critical_hit")
   ```

4. **Не дублируйте переводы**
   ```yaml
   # ❌ Плохо
   menu:
     continue: "Продолжить"
   game:
     continue: "Продолжить"  # Дубликат!
   
   # ✅ Хорошо
   menu:
     continue: "Продолжить"
   game:
     resume: "Возобновить"  # Другой контекст = другой ключ
   ```

---

## Примеры для разработчиков

### Создание простого мода

```bash
# 1. Создать структуру
mkdir -p data/mods/my_mod
cd data/mods/my_mod

# 2. Создать mod.yaml
cat > mod.yaml << EOF
name: "My First Mod"
name_localized: "mod.my_mod.name"
version: "1.0.0"
author: "Me"
description_localized: "mod.my_mod.description"
compatible_version: "0.1.0"
adds:
  items: true
EOF

# 3. Создать localization.yaml
cat > localization.yaml << EOF
ru:
  mod:
    my_mod:
      name: "Мой Первый Мод"
      description: "Мой первый мод для игры"
  items:
    cool_sword:
      name: "Крутой меч"
      description: "Очень крутой меч!"
EOF
```

### Использование в приключении

```python
# В коде приключения

# Загрузить локализацию
from src.data.localization import loc

# Получить название локации
location_name = loc("locations.tavern.name")

# Показать описание с параметрами
npc_name = loc("npcs.bartender.name")
description = loc("locations.tavern.description", 
                 bartender=npc_name)

# Диалог
dialog_text = loc("dialogues.bartender_greeting.text")
option1 = loc("dialogues.bartender_greeting.option1")
```

---

## FAQ

**Q: Обязательна ли локализация для модов?**  
A: Да! Каждый мод должен иметь `localization.yaml` с переводами.

**Q: Можно ли использовать только русский язык?**  
A: Технически да, но рекомендуется поддерживать минимум `ru` и `en`.

**Q: Что если ключ не найден?**  
A: Вернётся `[key]` или значение `default` если указано.

**Q: Как переопределить базовый перевод?**  
A: Просто используйте тот же ключ в локализации мода/приключения.

**Q: Работает ли кэширование со строками с параметрами?**  
A: Нет, форматированные строки не кэшируются.

---

## Заключение

Система локализации спроектирована так, чтобы:
- ✅ Быть простой в использовании
- ✅ Не дублировать код (DRY)
- ✅ Легко расширяться (моды, приключения)
- ✅ Поддерживать приоритеты источников
- ✅ Обеспечивать производительность (кэш)

Следуйте этой документации при создании модов и приключений!