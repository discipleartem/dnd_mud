# Автоматический роутинг запросов к агентам

**Дата создания:** 2025-01-27  
**Статус:** ✅ Реализовано

## 🎯 Цель

Автоматическое определение нужного агента для каждого запроса пользователя без необходимости явного указания агента.

## 🔄 Как это работает

### Принцип работы

1. **Пользователь отправляет запрос** (например: "Проверь код в combat.py")
2. **Главный агент анализирует запрос** и автоматически определяет нужного агента
3. **Агент запускается автоматически** через MCP без явного указания
4. **Результаты возвращаются** пользователю

### Алгоритм автоматического роутинга

Главный агент использует следующие правила для определения агента:

| Тип запроса | Ключевые слова | Агент |
|-------------|----------------|-------|
| **Code Review** | проверь, ревью, баг, ошибка, проблема, производительность, оптимизация, анализ кода | `code-reviewer` |
| **Тесты** | тест, тесты, покрытие, coverage, unit test, integration test, pytest, тестирование | `test-writer` |
| **Безопасность** | безопасность, security, уязвимость, vulnerability, injection, валидация, безопасный код | `security-checker` |
| **Документация** | документация, docstring, документация, комментарий, описание, README, API docs | `doc-writer` |
| **Архитектура** | архитектура, структура, паттерн, рефакторинг, дизайн, масштабируемость, проектирование | `architect` |

## 📋 Примеры использования

### Пример 1: Простой запрос на проверку кода

**Запрос пользователя:**
```
Проверь код в combat.py
```

**Автоматический роутинг:**
- Определён агент: `code-reviewer`
- Запрос для агента: "Проверь файл src/systems/combat.py на корректность игровой механики D&D 5e, производительность и потенциальные баги."

**Результат:**
- Агент автоматически запускается через MCP
- Результаты возвращаются пользователю

### Пример 2: Запрос на создание тестов

**Запрос пользователя:**
```
Нужны тесты для системы боя
```

**Автоматический роутинг:**
- Определён агент: `test-writer`
- Запрос для агента: "Создай comprehensive тесты для src/systems/combat.py. Покрой happy path, edge cases для боевой системы и error handling."

### Пример 3: Комплексная задача

**Запрос пользователя:**
```
Добавь новую систему магии, проверь безопасность и создай тесты
```

**Автоматический роутинг:**
- Определена последовательность:
  1. `architect` - проектирование архитектуры системы магии
  2. `security-checker` - проверка безопасности реализации
  3. `test-writer` - создание тестов

## 🛠️ Реализация

### Созданные компоненты

1. **Агент-роутер** (`docs/agents/agent-router.md`)
   - Определяет нужного агента на основе анализа запроса
   - Содержит правила роутинга и примеры

2. **Обновлённые правила главного агента**
   - `docs/user_rules.md` - добавлен раздел автоматического роутинга
   - `.cursor/rules/user.mdc` - обновлены правила для Cursor IDE

### Интеграция с MCP

Автоматический роутинг работает через MCP инструмент `mcp_sub-agents_run_agent`:

```python
# Главный агент автоматически определяет агента и запускает его
mcp_sub-agents_run_agent(
    agent="code-reviewer",  # Определено автоматически
    prompt="Проверь файл src/systems/combat.py..."  # Сформировано автоматически
)
```

## ✅ Преимущества

1. **Удобство:** Пользователю не нужно указывать агента явно
2. **Точность:** Автоматическое определение агента на основе анализа запроса
3. **Эффективность:** Меньше шагов для выполнения задачи
4. **Консистентность:** Единый подход к роутингу запросов

## 🔍 Диагностика

### Если роутинг работает неправильно

1. **Проверьте ключевые слова в запросе:**
   - Убедитесь, что запрос содержит ключевые слова для определения типа задачи
   - Используйте более конкретные формулировки

2. **Проверьте доступность агентов:**
   ```bash
   ls -la /home/tomas/WORK/dnd_mud/docs/agents/*.md
   ```

3. **Проверьте логи MCP:**
   - Help → Toggle Developer Tools
   - Проверьте консоль на ошибки роутинга

### Переопределение автоматического роутинга

Если нужно явно указать агента, используйте формулировку:
```
Используй [имя_агента] для [задача]
```

Например:
```
Используй code-reviewer для проверки combat.py
```

## 📚 Дополнительная документация

- `docs/agents/agent-router.md` - полное описание агента-роутера
- `docs/user_rules.md` - правила автоматического роутинга
- `docs/project_rules.md` - общая архитектура мульти-агентности

---

**Статус:** ✅ Автоматический роутинг реализован и готов к использованию  
**Дата:** 2025-01-27

