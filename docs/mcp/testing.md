# Тест работоспособности мульти-агентности

**Дата проверки:** 2025-01-27  
**Статус:** ✅ Все зависимости установлены, готово к тестированию

## ✅ Проверка зависимостей

### Результаты проверки:

- ✅ **Node.js:** v20.19.6 установлен
- ✅ **npx:** 10.8.2 установлен
- ✅ **cursor-agent:** 2025.11.25-d5b3271 установлен и аутентифицирован
- ✅ **Конфигурация MCP:** валидна
- ✅ **Определения агентов:** 5 агентов найдено и проверено
- ✅ **Разрешения:** настроены корректно

## 🧪 Тестирование мульти-агентности

### Тест 1: Простой запуск агента

**Команда для тестирования в Cursor IDE:**

```
Используй code-reviewer для проверки файла src/systems/combat.py на корректность игровой механики D&D 5e и производительность
```

**Ожидаемый результат:**
- Агент должен запуститься через MCP сервер
- Должен быть создан файл сессии в `docs/agents/sessions/`
- Агент должен проанализировать код и вернуть результаты

### Тест 2: Мульти-агентный workflow

**Последовательность тестов:**

1. **Code Reviewer:**
   ```
   Используй code-reviewer для проверки файла src/systems/combat.py
   Фокус на: корректность игровой механики D&D 5e, производительность, обработка ошибок
   ```

2. **Test Writer:**
   ```
   Используй test-writer для создания тестов для src/systems/combat.py
   Покрыть: happy path, edge cases для боевой системы, error handling
   ```

3. **Security Checker:**
   ```
   Используй security-checker для проверки безопасности src/systems/combat.py
   Фокус на: валидация пользовательского ввода, безопасность загрузки данных
   ```

4. **Doc Writer:**
   ```
   Используй doc-writer для добавления docstrings в src/systems/combat.py
   Фокус на: публичные методы боевой системы, примеры использования
   ```

### Тест 3: Проверка сессий

После запуска агентов проверьте создание сессий:

```bash
ls -la /home/tomas/WORK/dnd_mud/docs/agents/sessions/
```

Должны создаваться файлы сессий для каждого запуска агента.

## 📊 Ожидаемое поведение

### При успешном запуске:

1. **MCP сервер доступен:**
   - Cursor IDE должен видеть MCP сервер `sub-agents`
   - Доступны команды для запуска агентов

2. **Агенты запускаются:**
   - Агент получает контекст проекта
   - Агент выполняет задачу
   - Результаты возвращаются через MCP

3. **Сессии создаются:**
   - Файлы сессий создаются в `docs/agents/sessions/`
   - Сессии сохраняют контекст между вызовами (если SESSION_ENABLED=true)

4. **Мульти-агентный workflow:**
   - Несколько агентов могут работать последовательно
   - Контекст передается между агентами через сессии

## 🔍 Диагностика проблем

### Если агенты не запускаются:

1. **Проверьте логи MCP сервера:**
   - В Cursor IDE откройте Developer Tools (Help → Toggle Developer Tools)
   - Проверьте консоль на наличие ошибок MCP

2. **Проверьте доступность MCP сервера:**
   - Откройте Command Palette (Ctrl+Shift+P)
   - Найдите команды связанные с MCP или `sub-agents`
   - Проверьте список доступных инструментов

3. **Проверьте аутентификацию:**
   ```bash
   cursor-agent status
   ```
   Должно показать информацию об аутентифицированном пользователе

4. **Проверьте конфигурацию:**
   ```bash
   ./scripts/test_mcp_config.sh
   ```

### Если появляются ошибки:

**Ошибка: "Agent not found"**
- Проверьте, что файл агента существует в `docs/agents/`
- Проверьте имя агента (должно совпадать с именем файла без расширения)

**Ошибка: "Authentication required"**
- Выполните `cursor-agent login`
- Или добавьте `CURSOR_API_KEY` в `~/.cursor/mcp.json`

**Ошибка: "Timeout"**
- Увеличьте `EXECUTION_TIMEOUT_MS` в `mcp.json`
- Упростите задачу агента

## ✅ Чек-лист тестирования

- [ ] Все зависимости установлены (Node.js, npx, cursor-agent)
- [ ] cursor-agent аутентифицирован
- [ ] Cursor IDE перезапущен после изменений
- [ ] MCP сервер доступен в Cursor IDE
- [ ] Простой тест запуска агента выполнен
- [ ] Мульти-агентный workflow протестирован
- [ ] Сессии создаются корректно
- [ ] Результаты агентов возвращаются через MCP

## 📋 Следующие шаги

1. **Протестируйте простой запуск агента:**
   - Используйте команду из "Тест 1" в Cursor IDE
   - Проверьте результаты

2. **Протестируйте мульти-агентный workflow:**
   - Выполните последовательность из "Тест 2"
   - Проверьте передачу контекста между агентами

3. **Проверьте создание сессий:**
   - Убедитесь, что файлы сессий создаются
   - Проверьте сохранение контекста между вызовами

4. **Документируйте результаты:**
   - Зафиксируйте успешные тесты
   - Отметьте любые проблемы и их решения

---

**Статус:** ✅ Готово к тестированию  
**Дата:** 2025-01-27

