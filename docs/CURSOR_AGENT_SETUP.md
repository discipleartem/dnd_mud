# Инструкция по настройке cursor-agent для MCP агентов

## ✅ Выполнено

1. ✅ `cursor-agent` CLI установлен
   - Версия: `2025.11.25-d5b3271`
   - Расположение: `/home/tomas/.local/bin/cursor-agent`
   - Добавлен в PATH через `~/.bashrc`

2. ✅ Конфигурация MCP обновлена
   - PATH добавлен в `~/.cursor/mcp.json`
   - MCP сервер теперь может найти `cursor-agent`

## ⚠️ Требуется действие

### Аутентификация cursor-agent

Для работы агентов требуется аутентификация. Выполните следующую команду в терминале:

```bash
export PATH="$HOME/.local/bin:$PATH"
cursor-agent login
```

Эта команда:
- Откроет браузер для аутентификации (или покажет ссылку)
- Сохранит токен для последующего использования
- Позволит агентам работать через MCP

### Альтернатива: использование CURSOR_API_KEY

Если у вас есть API ключ Cursor, вы можете добавить его в конфигурацию MCP вместо интерактивного login:

1. Получите API ключ из панели управления Cursor (https://cursor.com/settings)
2. Добавьте в `~/.cursor/mcp.json` в секцию `env`:

```json
{
  "mcpServers": {
    "sub-agents": {
      "env": {
        "CURSOR_API_KEY": "ваш_api_ключ",
        "PATH": "/home/tomas/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
      }
    }
  }
}
```

3. Перезапустите Cursor IDE для применения изменений

**Примечание:** `cursor-agent` также поддерживает флаг `--api-key` при запуске, но для MCP лучше использовать переменную окружения.

## 🧪 Проверка после аутентификации

После выполнения `cursor-agent login`, проверьте:

```bash
# Проверка статуса
export PATH="$HOME/.local/bin:$PATH"
cursor-agent status
# Должно показать: "Logged in" или подобное

# Перезапустите Cursor IDE, чтобы MCP сервер подхватил изменения
```

## 📋 Следующие шаги

1. Выполните `cursor-agent login` в терминале
2. Перезапустите Cursor IDE
3. Протестируйте запуск агента через MCP
4. Проверьте мульти-агентный workflow

## 🔍 Текущий статус

| Компонент | Статус |
|-----------|--------|
| cursor-agent установлен | ✅ |
| PATH настроен | ✅ |
| MCP конфигурация обновлена | ✅ |
| Аутентификация | ⏳ Требуется `cursor-agent login` |
| Запуск агентов | ⏳ После аутентификации |

