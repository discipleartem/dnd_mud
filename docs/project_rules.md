# Project Rules - DnD MUD Game с мульти-агентной архитектурой MCP

## 🌐 Язык общения

**ОБЯЗАТЕЛЬНО:** Все общение, комментарии, документация и взаимодействие с пользователем должны быть **только на русском языке**. Это правило применяется ко всем агентам и главному координатору без исключений.

## 🎯 Роль и контекст

Ты - эксперт в Python разработке игр со специализацией на текстовых RPG и MUD-играх. Проект - это **текстовая однопользовательская MUD-игра** на основе Dungeons & Dragons 5 Edition (2023), реализованная на Python 3.13+ с использованием Rich для UI и YAML для конфигурации. Проект использует **мульти-агентный подход** с локальными MCP (Model Context Protocol) серверами для разделения ответственности между специализированными AI агентами.



## 🏗️ Мульти-агентная архитектура

### Философия проекта

**Изоляция контекста**
- Каждый агент запускается с нуля без остаточного состояния
- Агенты получают только релевантную информацию для своей задачи
- Нет утечки контекста между запусками
- Главный агент остаётся фокусированным и лёгким

**Точность и надёжность**
- Агенты специализируются на одной цели без помех
- Минимальный риск путаницы из-за нерелевантного контекста
- Консистентные результаты в сложных многошаговых workflow

**Масштабируемость**
- Большие задачи безопасно разбиваются на подзадачи
- Каждый агент работает в пределах своего токен-лимита
- Главный агент координирует без достижения глобальных лимитов контекста

### Иерархия агентов

```
Main Agent (Главный координатор)
    ├── Code Reviewer (Ревьюер кода)
    ├── Test Writer (Писатель тестов)
    ├── Security Checker (Проверка безопасности)
    ├── Documentation Writer (Документация)
    ├── Architect (Архитектор)
    └── Custom Domain Agents (Доменные агенты)
```

### Поток работы

1. **Пользователь** отправляет запрос главному агенту
2. **Главный агент** анализирует задачу и определяет нужных специалистов
3. **MCP сервер** читает определение агента (markdown файл)
4. **Execution Engine** (cursor-agent) запускает агента
5. **Специализированный агент** выполняет задачу в изолированном контексте
6. **Результаты** возвращаются через MCP сервер
7. **Главный агент** получает результаты и продолжает работу



## ⚙️ Конфигурация MCP серверов

### Базовая структура mcp.json

**Расположение конфигурации:**
- **Cursor:** `~/.cursor/mcp.json`

```json
{
  "mcpServers": {
    "sub-agents": {
      "command": "npx",
      "args": ["-y", "sub-agents-mcp"],
      "env": {
        "AGENTS_DIR": "/absolute/path/to/docs/agents",
        "AGENT_TYPE": "cursor",
        "EXECUTION_TIMEOUT_MS": "600000",
        "SESSION_ENABLED": "true",
        "SESSION_DIR": "/absolute/path/to/sessions",
        "SESSION_RETENTION_DAYS": "1",
        "DEFAULT_MODEL": "claude-sonnet-4.5"
      }
    }
  }
}
```

### Настройка моделей для агентов

Модели можно настроить несколькими способами:

**1. Глобальная настройка (DEFAULT_MODEL)**
- Устанавливает модель по умолчанию для всех агентов
- Пример: `"DEFAULT_MODEL": "claude-sonnet-4.5"`

**2. Настройка через определение агента**
- В файле определения агента указать рекомендуемую модель
- Агент будет использовать указанную модель при доступности

**3. Переопределение через переменные окружения**
- Можно задать модель для конкретного агента через env переменные
- Формат: `AGENT_<NAME>_MODEL=model-name`

**Доступные модели:**
- `claude-sonnet-4.5` - Claude Sonnet 4.5 (рекомендуется)
- `gpt-4-turbo` - GPT-4 Turbo
- `gpt-4` - GPT-4
- `claude-opus-4` - Claude Opus 4 (для сложных задач)
- `o1-preview` - O1 Preview (для логических задач)

### Обязательные переменные окружения

**AGENTS_DIR**
- Абсолютный путь к папке с определениями агентов
- ✅ `/home/user/project/agents`
- ❌ `./agents` или `~/agents`

**AGENT_TYPE**
- `"cursor"` - использует cursor-agent CLI


### Опциональные настройки

**EXECUTION_TIMEOUT_MS** - максимальное время выполнения агента (по умолчанию: 5 минут, максимум: 10 минут)

**SESSION_ENABLED** - `"true"` включает управление сессиями, `"false"` каждый запуск с чистого листа

**SESSION_DIR** - абсолютный путь для хранения файлов сессий

**SESSION_RETENTION_DAYS** - количество дней хранения файлов сессий (по умолчанию: 1)

---

## 👥 Определение агентов

### Структура директории агентов

```
./docs/agents/
├── code-reviewer.md
├── test-writer.md
├── security-checker.md
├── doc-writer.md
├── architect.md
└── backend/
    ├── api-developer.md
```

### Формат определения агента

Каждый файл `.md` или `.txt` становится агентом. Имя файла = имя агента.

```markdown
# Название агента

## Роль и специализация
[Чёткое описание роли агента]

## Рекомендуемая модель
[Модель AI с обоснованием выбора]

## Ответственность
- Пункт 1
- Пункт 2
- Пункт 3

## Контекст работы
[Описание границ и ограничений]

## Выходной формат
[Как должны выглядеть результаты]

## Примеры
[Конкретные примеры работы]
```

### Рекомендуемые модели для агентов

Каждый агент оптимизирован под определённый тип задач. Рекомендуемые модели подобраны на основе анализа требований к каждому агенту:

| Агент | Рекомендуемая модель | Альтернатива | Обоснование |
|-------|---------------------|--------------|-------------|
| **Code Reviewer** | Claude Sonnet 4.5 | GPT-4 Turbo | Глубокий анализ кода, выявление багов, понимание игровых механик |
| **Test Writer** | GPT-4 Turbo | Claude Sonnet 4.5 | Отличная генерация тестов, понимание паттернов тестирования |
| **Security Checker** | Claude Sonnet 4.5 | GPT-4 Turbo | Понимание безопасности, поиск уязвимостей, знание стандартов |
| **Doc Writer** | Claude Sonnet 4.5 | GPT-4 Turbo | Качественная документация, ясные объяснения, структурированность |
| **Architect** | Claude Sonnet 4.5 | o1-preview* | Архитектурное мышление, понимание паттернов, trade-offs |

\* *o1-preview рекомендуется только для особенно сложных архитектурных задач, так как медленнее, но обеспечивает более глубокий логический анализ*

**Примечания:**
- **Claude Sonnet 4.5** - отличный баланс качества, скорости и стоимости для большинства задач
- **GPT-4 Turbo** - быстрая и эффективная модель для генерации кода и тестов
- **o1-preview** - для сложных логических задач, но медленнее и дороже
- Модели можно переопределить в конфигурации MCP сервера при необходимости

**Подробная документация:** `./docs/agents/MODELS.md` - сравнение моделей, рекомендации, настройка


## 🔄 Использование агентов

### Многошаговые workflow

```
1. Architect → Спроектировать структуру новой фичи
2. Code Reviewer → Проверить существующий код на соответствие
3. Test Writer → Создать тесты для новой фичи
4. Security Checker → Проверить безопасность
5. Documentation Writer → Задокументировать изменения и/или изменить существующую
```


## 🐍 Ключевые принципы Python Game Development

### 1. Архитектура кода

- Объектно-ориентированное программирование с использованием паттернов проектирования
- Многослойная архитектура: UI Layer → Core Layer → Systems Layer → Data Layer
- Использование Singleton для менеджеров (Game, StateManager, WindowManager)
- Предпочитай итерацию и модуляризацию вместо дублирования


### 2. Стиль кодирования

- Описательные имена переменных с вспомогательными глаголами (is_active, has_permission, can_access)
- Lowercase с подчёркиваниями для файлов и директорий (character.py, state_manager.py, main_menu.py)
- Одна ответственность на файл/модуль (максимум 1600 строк на файл)
- Использование Rich для всех UI элементов (Panel, Console, Text, Align)

### 3. Типизация и валидация

- Обязательные type hints для всех функций и методов
- Использовать Pydantic или другой инструмент для валидации данных
- Явная типизация возвращаемых значений (включая None)

### 4. Структура функций

```python
# Правильно:
def process_character_action(character: Character, action: str) -> ActionResult:
    # 1. Валидация и ранний возврат при ошибках
    if not character or not action:
        raise ValueError("Character and action required")
    
    # 2. Основная логика
    result = character.perform_action(action)
    
    # 3. Happy path в конце
    return ActionResult(result)
```

### 5. Обработка ошибок

- Обрабатывать ошибки в начале функции
- Использовать early returns для условий ошибок
- Избегать глубокой вложенности через guard clauses
- Логирование всех критических ошибок
- Пользовательские типы ошибок для консистентности
- Graceful degradation при ошибках загрузки данных (YAML, сохранения)
- Атомарное сохранение (сначала во временный файл, затем переименование)
- Проверка версий сохранений при загрузке



## 🧪 Тестирование

### Структура тестов

- pytest как основной фреймворк
- Тесты в директории ./tests
- Разделение на unit/integration
- Fixtures для повторяющихся данных (персонажи, игровые состояния)
- Mock'и для Rich Console и файловых операций
- Тестирование игровых систем (бросок кубиков, бой, магия)


## 🔐 Безопасность

### Безопасность игрового проекта

- Валидация пользовательского ввода (команды, имена персонажей)
- Безопасная загрузка YAML файлов (ограничение размера, валидация структуры)
- Защита сохранений от повреждения (атомарное сохранение, backup)
- Проверка версий сохранений при загрузке
- Обработка некорректных данных модов и приключений

### Настройка разрешений для MCP агентов

Агенты могут выполнять shell команды. Настройте разрешения:

**Cursor:** `<project>/.cursor/cli.json`

```json
{
  "permissions": {
    "allow": [
      "Shell(cd)",
      "Shell(ls)",
      "Shell(cat)",
      "Shell(git)",
      "Shell(npm)",
      "Shell(python)",
      "Shell(pytest)",
      "Shell(black)",
      "Shell(flake8)"
    ],
    "deny": [
      "Shell(rm -rf)",
      "Shell(sudo)",
      "Shell(chmod)"
    ]
  }
}
```


### Переменные для отладки MCP

```json
{
  "env": {
    "DEBUG_CURSOR_MCP": "true",
    "CURSOR_AGENT_IDLE_EXIT_MS": "0",
    "LOG_LEVEL": "debug"
  }
}
```


## 📦 Зависимости

### Основные зависимости

- **rich>=14.2.0** - UI библиотека для терминала (панели, таблицы, форматирование)
- **pyyaml>=6.0.3** - работа с YAML конфигурациями (настройки, локализация, моды)

### Development

- pytest для тестирования
- black для форматирования
- flake8 для линтинга
- mypy для проверки типов
- cx_Freeze для создания исполняемых файлов

### MCP Infrastructure

- npx для запуска sub-agents-mcp
- cursor-agent CLI (для Cursor)
- Node.js 20+ для MCP серверов


## 📂 Структура проекта

```
dnd_mud/
├── src/
│   ├── core/              # Ядро игры (Game, StateManager, WindowManager)
│   ├── ui/                # Интерфейс (MainMenu, MenuBase, Renderer)
│   ├── systems/           # Игровые системы (Character, Combat, Magic, Inventory)
│   ├── data/              # Загрузчики данных (Config, Loader, Localization)
│   └── utils/             # Утилиты (helpers, validators)
├── data/
│   ├── yaml/              # YAML конфигурации (settings, localization)
│   ├── saves/             # Сохранения игры
│   ├── mods/              # Пользовательские моды
│   └── adventures/        # Пользовательские приключения
├── tests/                 # Тесты
└── docs/                  # Документация
    └── agents/            # Определения MCP агентов
```

### Ключевые компоненты

- **StateManager** - управление состояниями игры (State паттерн)
- **WindowManager** - управление терминалом (размер, перенос текста)
- **LocalizationManager** - система локализации с приоритетами (моды > база)
- **MenuBase** - базовый класс для всех меню (Template Method)
- **Rich UI** - все UI элементы через Rich библиотеку



## 🚀 Best Practices

### Дизайн агентов

1. **Одна ответственность** - агент должен делать одну вещь хорошо
2. **Чёткие границы** - явно определить что агент делает и не делает
3. **Реалистичные ожидания** - агент не может заменить человека полностью
4. **Итеративный подход** - начните с простых агентов, усложняйте по мере необходимости

### Организация агентов

- Группируйте связанных агентов в папки
- Используйте префиксы для категоризации (backend-, test-, security-)
- Версионируйте определения агентов в Git
- Документируйте изменения в определениях

### Оптимизация производительности

- Используйте SESSION_ENABLED только когда нужен контекст между вызовами
- Настройте EXECUTION_TIMEOUT_MS адекватно задачам
- Очищайте старые сессии регулярно
- Используйте focused prompts для специфичных задач



## ⚙️ Troubleshooting

### Агент не найден

```
Error: Agent 'code-reviewer' not found
```

**Решение:**
- Проверьте что `AGENTS_DIR` указывает на правильную директорию (абсолютный путь)
- Убедитесь что файл `code-reviewer.md` существует
- Проверьте расширение файла (`.md` или `.txt`)

### Timeout ошибки

```
Error: Agent execution timed out
```

**Решение:**
- Увеличьте `EXECUTION_TIMEOUT_MS` в mcp.json
- Упростите задачу агента
- Разбейте на несколько меньших задач

### Permission denied

```
Error: Shell(pytest) permission denied
```

**Решение:**
1. Запустите CLI напрямую: `cursor-agent` 
2. Одобрите команды при появлении prompt
3. Или настройте вручную в `cli.json` / `settings.json`

### Authentication errors

```
Error: Not authenticated
```

**Решение:**
```bash
# Для Cursor
cursor-agent login
```

## 📚 Ресурсы

- [Model Context Protocol (MCP)](https://modelcontextprotocol.io/)
- [sub-agents-mcp GitHub](https://github.com/shinpr/sub-agents-mcp)
- [agentic-cursorrules GitHub](https://github.com/s-smits/agentic-cursorrules)
- [Cursor MCP Documentation](https://docs.cursor.com/context/model-context-protocol)
- [Awesome Cursor Rules](https://github.com/PatrickJS/awesome-cursorrules)
