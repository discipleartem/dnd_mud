# Архитектурная диаграмма D&D MUD (текущее состояние)

## Проблемы в текущей архитектуре

### 🚨 Критические нарушения Clean Architecture

```
┌─────────────────┐    ┌─────────────────┐
│   UI Layer      │    │  Domain Layer   │
├─────────────────┤    ├─────────────────┤
│ UI Character    │◄──►│ Domain Character│
│ UI Race        │◄──►│ Domain Race     │
│ UI Abilities   │◄──►│ Domain Abilities│
└─────────────────┘    └─────────────────┘
        │                       │
        └───────────┬───────────┘
                    ▼
        ┌─────────────────────────────┐
        │  ДУПЛИКАЦИЯ ЛОГИКИ       │
        │  ЦИКЛИЧЕСКИЕ ЗАВИСИМОСТИ │
        └─────────────────────────────┘
```

### 📊 Анализ дублирования

| Сущность | Domain | UI | Проблема |
|----------|---------|----|----------|
| Character | ✅ Чистая бизнес-логика | ❌ Смешан с UI логикой | Дублирование 312 строк |
| Race | ✅ Чистая сущность | ❌ Зависит от сервисов | Дублирование 259 строк |
| Abilities | ❌ Отсутствует | ✅ UI энумерации | Нарушение разделения |
| Language | ✅ Чистая сущность | ❌ Дублируется | Дублирование логики |

### 🔗 Циклические зависимости

```
ui/entities/race.py → services/language_service.py → ui/entities/race.py
ui/entities/character.py → ui/adapters/character_adapter.py → ui/entities/character.py
ui/main_menu/new_game.py → ui/entities/character.py → ui/adapters/character_adapter.py
```

## Целевая архитектура (Clean Architecture)

```
┌─────────────────────────────────────────────────────────────────┐
│                    UI Layer (Presentation)                  │
├─────────────────────────────────────────────────────────────────┤
│  Controllers  │  DTOs  │  Adapters  │  View Models      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                 Use Cases Layer                             │
├─────────────────────────────────────────────────────────────────┤
│  CharacterCreation  │  LanguageManagement  │  Validation   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                 Domain Layer                                │
├─────────────────────────────────────────────────────────────────┤
│  Entities  │  Value Objects  │  Domain Services  │  Events  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              Interfaces Layer                                 │
├─────────────────────────────────────────────────────────────────┤
│  Repositories  │  Services  │  External APIs  │  Events   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│            Infrastructure Layer                             │
├─────────────────────────────────────────────────────────────────┤
│  YAML Repos  │  Factories  │  Loaders  │  Unit of Work   │
└─────────────────────────────────────────────────────────────────┘
```

## План миграции

### Этап 1: Удаление дубликатов
- [ ] Удалить `src/ui/entities/character.py`
- [ ] Удалить `src/ui/entities/race.py` 
- [ ] Удалить `src/ui/entities/abilities.py`
- [ ] Создать правильные DTO в `src/ui/dto/`

### Этап 2: Создание интерфейсов
- [ ] Дополнить `src/interfaces/repositories.py`
- [ ] Создать `src/interfaces/use_cases.py`
- [ ] Создать `src/interfaces/dto.py`

### Этап 3: Use Cases
- [ ] Создать `src/use_cases/character_creation.py`
- [ ] Создать `src/use_cases/language_management.py`
- [ ] Создать `src/use_cases/character_validation.py`

### Этап 4: Рефакторинг UI
- [ ] Обновить контроллеры для работы с Use Cases
- [ ] Создать адаптеры Domain → DTO
- [ ] Убрать бизнес-логику из UI

## Правила зависимостей после рефакторинга

```
✅ Правильные зависимости:
UI → Use Cases → Domain → Interfaces ← Infrastructure

❌ Запрещенные зависимости:
UI → Domain (напрямую)
Domain → Infrastructure
Use Cases → Infrastructure
UI → Infrastructure
```

## Критерии успеха

### Архитектурные метрики
- [ ] 0 дублирующихся сущностей
- [ ] Четкое разделение слоев
- [ ] Отсутствие циклических зависимостей
- [ ] Тестируемость каждого слоя

### Качество кода  
- [ ] 100% PEP 8 соответствие
- [ ] Полная типизация
- [ ] Покрытие тестами > 80%
- [ ] 0 критических проблем в mypy/ruff
