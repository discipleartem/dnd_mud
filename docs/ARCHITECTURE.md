# Архитектура D&D MUD

## Обзор

D&D MUD следует принципам **Clean Architecture** и использует модульную структуру для обеспечения тестируемости, расширяемости и поддерживаемости кода.

## 🏗️ Архитектурные слои

```
┌─────────────────────────────────────────────────────────────┐
│                    UI Layer (Презентация)                  │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │   Main Menu     │  │   Entities      │                 │
│  │   (new_game.py) │  │  (character.py) │                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                Service Layer (Бизнес-логика)               │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │ Language        │  │   Ability       │                 │
│  │ Service         │  │ Generation      │                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                Data Layer (Данные и сущности)              │
│  ┌─────────────────┐  ┌─────────────────┐                 │
│  │   YAML Files    │  │   Entity        │                 │
│  │  (races.yaml)   │  │  Classes        │                 │
│  └─────────────────┘  └─────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

## 📁 Структура директорий

```
dnd_mud/
├── 📂 src/                          # Исходный код
│   ├── 📂 services/                 # Сервисный слой
│   │   └── 📄 language_service.py   # Сервис языков
│   └── 📂 ui/                       # Слой представления
│       ├── 📂 entities/              # Сущности UI
│       │   ├── 📄 abilities.py      # Способности
│       │   ├── 📄 character.py      # Персонаж
│       │   ├── 📄 language_loader.py # Загрузчик языков
│       │   └── 📄 race.py           # Расы
│       └── 📂 main_menu/            # Главное меню
│           ├── 📄 ability_generation.py # Генерация характеристик
│           ├── 📄 load_game.py      # Загрузка игры
│           ├── 📄 main.py           # Главное меню
│           ├── 📄 new_game.py       # Новая игра
│           └── 📄 settings.py       # Настройки
├── 📂 data/                         # Игровые данные
│   ├── 📄 abilities.yaml           # Способности и навыки
│   ├── 📄 backgrounds.yaml         # Предыстории
│   ├── 📄 classes.yaml             # Классы персонажей
│   ├── 📄 languages.yaml           # Языки
│   └── 📄 races.yaml              # Расы и подрасы
├── 📂 localization/                # Локализация
│   ├── 📄 ru.yaml                  # Русский язык
│   └── 📄 en.yaml                  # Английский язык
├── 📂 tests/                        # Тесты
├── 📄 main.py                       # Точка входа
└── 📄 i18n.py                       # Интернационализация
```

## 🎯 Применяемые паттерны проектирования

### 1. Factory Pattern (Фабрика)

**Где используется:** `src/ui/entities/race.py`

```python
class Race:
    @classmethod
    def load_from_yaml(cls) -> Dict[str, 'Race']:
        """Фабричный метод для загрузки всех рас"""
        
    @classmethod
    def get_race(cls, race_id: str) -> 'Race':
        """Фабричный метод для создания конкретной расы"""
        
    @classmethod
    def _create_from_dict(cls, data: Dict) -> 'Race':
        """Приватный фабричный метод"""
```

**Преимущества:**
- Инкапсуляция логики создания объектов
- Единая точка создания рас
- Упрощение тестирования

### 2. Repository Pattern (Хранилище)

**Где используется:** Загрузка данных из YAML файлов

```python
class RaceRepository:
    def load_all() -> Dict[str, Race]:
        """Загрузить все расы из YAML"""
        
    def load_by_id(race_id: str) -> Race:
        """Загрузить расу по ID"""
```

**Преимущества:**
- Абстракция над источником данных
- Легкая замена источника (YAML → БД → API)
- Унифицированный интерфейс доступа к данным

### 3. Data Transfer Object (DTO)

**Где используется:** Все классы сущностей

```python
@dataclass
class Race:
    name: str
    description: str
    ability_bonuses: Dict[str, int]
    # ... другие поля
    
@dataclass
class Character:
    name: str
    race: Optional[Race] = None
    ability_scores: Optional[AbilityScores] = None
```

**Преимущества:**
- Четкое определение структуры данных
- Автоматическая валидация типов
- Простота сериализации/десериализации

### 4. Service Layer (Сервисный слой)

**Где используется:** `src/services/`

```python
class LanguageService:
    def get_available_languages() -> List[str]:
        """Получить доступные языки"""
        
    def get_race_languages(race_id: str) -> List[str]:
        """Получить языки расы"""
```

**Преимущества:**
- Изоляция бизнес-логики
- Переиспользование в разных частях приложения
- Упрощение тестирования

### 5. Strategy Pattern (Стратегия)

**Где используется:** Генерация характеристик

```python
class AbilityGenerationStrategy(ABC):
    @abstractmethod
    def generate(self) -> Dict[str, int]:
        pass

class StandardArrayStrategy(AbilityGenerationStrategy):
    def generate(self) -> Dict[str, int]:
        # Стандартный массив
        
class PointBuyStrategy(AbilityGenerationStrategy):
    def generate(self) -> Dict[str, int]:
        # Покупка очков
        
class RandomGenerationStrategy(AbilityGenerationStrategy):
    def generate(self) -> Dict[str, int]:
        # Случайная генерация
```

## 🔄 Поток данных

### Создание персонажа

```
1. UI Layer (new_game.py)
   ↓
2. Race.get_race() [Factory]
   ↓
3. YAML Loading [Repository]
   ↓
4. Race Entity [DTO]
   ↓
5. Ability Generation [Strategy]
   ↓
6. Character Entity [DTO]
   ↓
7. UI Display
```

### Локализация

```
1. UI Layer вызывает t('key')
   ↓
2. I18nManager [Singleton]
   ↓
3. YAML Loading (ru.yaml/en.yaml)
   ↓
4. String Formatting
   ↓
5. Return localized string
```

## 🧪 Тестируемость

### Изоляция зависимостей

```python
# Пример теста с моками
def test_character_creation():
    # Arrange
    mock_race = Race(name="Человек", ability_bonuses={"strength": 1})
    
    # Act
    character = Character(name="Тест", race=mock_race)
    
    # Assert
    assert character.name == "Тест"
    assert character.race.name == "Человек"
```

### Unit тесты

- **Entity тесты:** Проверка валидации данных
- **Service тесты:** Проверка бизнес-логики
- **UI тесты:** Проверка пользовательского ввода

### Integration тесты

- **Repository тесты:** Проверка загрузки из YAML
- **End-to-end тесты:** Полный процесс создания персонажа

## 🔧 Принципы SOLID

### Single Responsibility Principle
- Каждый класс имеет одну ответственность
- `Race` отвечает только за данные расы
- `AbilityGeneration` отвечает только за генерацию характеристик

### Open/Closed Principle
- Классы открыты для расширения, закрыты для изменения
- Новые стратегии генерации без изменения существующего кода
- Новые расы через YAML без изменения кода

### Liskov Substitution Principle
- Подклассы могут заменить базовые классы
- Все стратегии генерации взаимозаменяемы

### Interface Segregation Principle
- Множество специализированных интерфейсов
- Разделение интерфейсов загрузки данных и отображения

### Dependency Inversion Principle
- Зависимости от абстракций, а не от конкретных реализаций
- Использование абстрактных базовых классов

## 🚀 Расширяемость

### Добавление новой расы

1. Добавить данные в `data/races.yaml`
2. Добавить локализацию в `localization/ru.yaml`
3. Добавить тесты в `tests/`
4. **Без изменения кода!**

### Добавление нового метода генерации

1. Создать новый класс стратегии
2. Реализовать интерфейс `AbilityGenerationStrategy`
3. Добавить в фабрику стратегий
4. **Без изменения существующего кода!**

### Добавление нового языка

1. Создать `localization/{lang}.yaml`
2. Добавить язык в поддерживаемые
3. **Без изменения кода!**

## 📊 Метрики архитектуры

### Связность (Cohesion)
- **Высокая** - классы сфокусированы на одной задаче
- Модули имеют четкую ответственность

### Сцепление (Coupling)
- **Низкое** - минимальные зависимости между модулями
- Использование абстракций и интерфейсов

### Тестируемость
- **Высокая** - легкое мокирование зависимостей
- Изолированные unit тесты

## 🔮 Будущие улучшения

### Краткосрочные

1. **Dependency Injection Container** - управление зависимостями
2. **Event System** - loosely coupled коммуникация
3. **Configuration Management** - централизованная конфигурация

### Долгосрочные

1. **Plugin System** - динамическая загрузка модулей
2. **Database Abstraction** - поддержка разных хранилищ
3. **API Layer** - REST/GraphQL интерфейс

---

**D&D MUD Architecture** - чистая, масштабируемая и тестируемая архитектура
