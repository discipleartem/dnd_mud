# 🔧 Техническая документация DnD MUD Game

## Оглавление
1. [Архитектура проекта](#архитектура-проекта)
2. [Технологический стек](#технологический-стек)
3. [Структура модулей](#структура-модулей)
4. [Паттерны проектирования](#паттерны-проектирования)
5. [Форматы данных](#форматы-данных)
6. [API документация](#api-документация)

### src/data/ - Загрузка и управление данными

#### localization.py
```python
class LocalizationManager:
    """
    Менеджер локализации с поддержкой множественных источников.
    
    Паттерны:
    - Singleton для единственного экземпляра
    - Chain of Responsibility для поиска переводов
    - Strategy для разных источников
    """
```

**Ответственность:**
- Загрузка базовой локализации
- Загрузка локализации модов
- Загрузка локализации приключений
- Приоритизация источников (приключения > моды > база)
- Кэширование переводов

**Структура приоритетов:**
```
Приключения (priority 200) ← Высший приоритет
    ↓
Моды (priority 100)
    ↓
Базовая локализация (priority 0) ← Низший приоритет
```

**Использование:**
```python
from src.data.localization import loc

# Простое получение
text = loc("menu.continue")

# С параметрами форматирования
text = loc("combat.hit", target="Goblin", damage=5)

# С fallback
text = loc("unknown.key", default="Default text")
```

#### config.py
```python
class ConfigManager:
    """
    Менеджер конфигурации игры.
    
    Загружает настройки из YAML файлов.
    """
```

---

## Архитектура проекта

### Общая архитектура

Проект использует **многослойную архитектуру** с разделением ответственности:

```
┌─────────────────────────────────────┐
│         UI Layer (Presentation)      │
│   ┌─────────────────────────────┐   │
│   │  MainMenu, Renderer, etc    │   │
│   └─────────────────────────────┘   │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      Core Layer (Business Logic)    │
│   ┌─────────────────────────────┐   │
│   │  Game, StateManager, etc    │   │
│   └─────────────────────────────┘   │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│     Systems Layer (Game Systems)    │
│   ┌─────────────────────────────┐   │
│   │ Combat, Magic, Inventory    │   │
│   └─────────────────────────────┘   │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│      Data Layer (Storage)           │
│   ┌─────────────────────────────┐   │
│   │  YAML, JSON, Loaders        │   │
│   └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### Принципы проектирования

#### SOLID принципы

1. **Single Responsibility Principle (SRP)**
   - Каждый класс отвечает за одну функциональность
   - Пример: `SaveSystem` только для сохранений, `Combat` только для боя

2. **Open/Closed Principle (OCP)**
   - Классы открыты для расширения, закрыты для модификации
   - Использование наследования и композиции для новой функциональности

3. **Liskov Substitution Principle (LSP)**
   - Подклассы могут использоваться вместо базовых классов
   - Все меню наследуются от `MenuBase`

4. **Interface Segregation Principle (ISP)**
   - Интерфейсы специфичны для клиентов
   - Разделение на мелкие, специализированные классы

5. **Dependency Inversion Principle (DIP)**
   - Зависимость от абстракций, а не от конкретных реализаций
   - Использование dependency injection

#### Дополнительные принципы

- **KISS** (Keep It Simple, Stupid) - простота решений
- **DRY** (Don't Repeat Yourself) - избегание дублирования кода
- **YAGNI** (You Aren't Gonna Need It) - реализация только нужного функционала
- **Бритва Оккама** - предпочтение простейших решений

---

## Технологический стек

### Основной стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| Python | 3.13+ | Основной язык программирования |
| Rich | 14.2+ | UI библиотека для терминала |
| PyYAML | 6.0+ | Работа с YAML файлами |
| cx_Freeze | 8.5+ | Создание исполняемых файлов |

### Инструменты разработки

| Инструмент | Назначение |
|------------|------------|
| pytest | Тестирование |
| black | Форматирование кода |
| flake8 | Линтинг |
| mypy | Статическая типизация |
| GitHub Actions | CI/CD |

---

## Структура модулей

### src/core/ - Ядро игры

#### game.py
```python
class Game:
    """
    Главный класс игры, координирующий все системы.
    
    Принципы:
    - Singleton паттерн для единого экземпляра игры
    - Управление жизненным циклом игры
    - Координация между всеми подсистемами
    """
```

**Ответственность:**
- Инициализация всех систем
- Главный игровой цикл
- Координация между UI и игровыми системами

#### state_manager.py
```python
class StateManager:
    """
    Управление состоянием игры.
    
    Паттерны:
    - State паттерн для управления состояниями
    - Memento для сохранения/восстановления состояния
    """
```

**Ответственность:**
- Хранение текущего состояния игры
- Переходы между состояниями (меню, игра, бой и т.д.)
- Интеграция с системой сохранений

**Состояния игры:**
```python
class GameState(Enum):
    MAIN_MENU = "main_menu"
    CHARACTER_CREATION = "character_creation"
    ADVENTURE = "adventure"
    COMBAT = "combat"
    INVENTORY = "inventory"
    REST = "rest"
    SETTINGS = "settings"
```

#### window_manager.py
```python
class WindowManager:
    """
    Управление окном терминала.
    
    Паттерны:
    - Singleton для единого менеджера окна
    - Observer для обработки изменения размера
    """
```

**Ответственность:**
- Определение размера терминала
- Обработка resize событий
- Автоматический перенос текста
- Проверка минимального размера (80x24)

**Важные методы:**
```python
def get_terminal_size() -> Tuple[int, int]:
    """Возвращает (ширина, высота) терминала"""

def wrap_text(text: str, width: int) -> List[str]:
    """Переносит текст по ширине"""

def check_minimum_size() -> bool:
    """Проверяет минимальный размер 80x24"""
```

---

### src/ui/ - Интерфейс пользователя

#### menu_base.py
```python
class MenuBase(ABC):
    """
    Базовый класс для всех меню.
    
    Паттерны:
    - Template Method для общей структуры меню
    - Strategy для разных способов ввода
    """
    
    @abstractmethod
    def render(self) -> None:
        """Отрисовка меню"""
    
    @abstractmethod
    def handle_input(self, user_input: str) -> Any:
        """Обработка ввода пользователя"""
```

#### main_menu.py
```python
class MainMenu(MenuBase):
    """
    Главное меню игры.
    
    Функции:
    - Отображение заголовка игры
    - Пункты меню: Продолжить, Новая игра, Загрузка, Настройки, Выход
    - Обработка выбора пользователя
    """
```

**Структура главного меню:**
```
╔════════════════════════════════════════╗
║                                        ║
║   Dungeons and Dragons 5e (2023)       ║
║                                        ║
╠════════════════════════════════════════╣
║                                        ║
║   1. Продолжить                        ║
║   2. Новая игра                        ║
║   3. Загрузить                         ║
║   4. Настройки                         ║
║   5. Выход                             ║
║                                        ║
╚════════════════════════════════════════╝

Выберите пункт меню:
```

#### renderer.py
```python
class Renderer:
    """
    Отрисовка элементов UI через Rich.
    
    Паттерны:
    - Facade для упрощения работы с Rich
    - Builder для построения сложных UI элементов
    """
```

**Функции:**
- Отрисовка рамок и панелей
- Форматирование текста
- Таблицы и списки
- Прогресс-бары

---

### src/systems/ - Игровые системы

#### character.py
```python
class Character:
    """
    Класс персонажа игрока.
    
    Атрибуты D&D 5e:
    - Характеристики (STR, DEX, CON, INT, WIS, CHA)
    - Класс и уровень
    - Хиты и Hit Dice
    - Навыки и умения
    """
```

#### dice.py
```python
class Dice:
    """
    Система бросков кубиков.
    
    Поддержка:
    - Все типы кубиков (d4, d6, d8, d10, d12, d20, d100)
    - Advantage/Disadvantage
    - Модификаторы
    - История бросков
    """
    
    @staticmethod
    def roll(dice_notation: str) -> DiceResult:
        """
        Бросок по нотации: "2d6+3", "1d20", "3d8-2"
        Возвращает результат с деталями броска
        """
```

#### combat.py
```python
class CombatSystem:
    """
    Система боя по правилам D&D 5e.
    
    Механики:
    - Инициатива
    - Порядок ходов
    - Атаки и урон
    - Состояния (conditions)
    - Action economy
    """
```

#### magic.py
```python
class MagicSystem:
    """
    Система магии D&D 5e.
    
    Функции:
    - Подготовка заклинаний
    - Spell slots
    - Каст заклинаний
    - Концентрация
    """
```

#### inventory.py
```python
class Inventory:
    """
    Система инвентаря.
    
    Механики:
    - Хранение предметов
    - Вес и ограничения
    - Экипировка
    - Использование предметов
    """
```

#### time.py
```python
class TimeSystem:
    """
    Внутриигровое время.
    
    Функции:
    - Календарь
    - Время суток
    - Погода
    - Эффекты времени
    """
```

#### rest.py
```python
class RestSystem:
    """
    Система отдыха D&D 5e.
    
    Типы отдыха:
    - Short Rest (1 час)
    - Long Rest (8 часов)
    """
```

#### save_system.py
```python
class SaveSystem:
    """
    Система сохранений.
    
    Типы сохранений:
    - Автосохранения (10 слотов с ротацией)
    - Пользовательские сохранения
    - Быстрое сохранение
    """
```

**Формат сохранения (JSON):**
```json
{
    "version": "0.1.0",
    "timestamp": "2024-01-15T10:30:00",
    "character": {
        "name": "Aragorn",
        "class": "Fighter",
        "level": 5,
        "stats": {...}
    },
    "game_state": {...},
    "inventory": {...},
    "location": "tavern_main"
}
```

---

## Паттерны проектирования

### Используемые паттерны

#### Порождающие (Creational)

1. **Singleton**
   - `Game` - единственный экземпляр игры
   - `WindowManager` - единственный менеджер окна
   - `SaveSystem` - единственная система сохранений

2. **Factory Method**
   - `CharacterFactory` - создание персонажей разных классов
   - `ItemFactory` - создание предметов

3. **Builder**
   - `CharacterBuilder` - пошаговое создание персонажа
   - `UIBuilder` - построение сложных UI элементов

#### Структурные (Structural)

1. **Facade**
   - `Renderer` - упрощённый интерфейс для Rich
   - `DataLoader` - упрощение загрузки данных

2. **Adapter**
   - `YAMLAdapter` - адаптация YAML данных к внутреннему формату

3. **Composite**
   - Система меню - древовидная структура

#### Поведенческие (Behavioral)

1. **State**
   - `StateManager` - управление состояниями игры

2. **Strategy**
   - Разные способы ввода команд
   - Разные стратегии боя для AI

3. **Observer**
   - События изменения размера окна
   - События в боевой системе

4. **Command**
   - Система команд (/help, /go, /take)

5. **Template Method**
   - `MenuBase` - шаблон для всех меню

6. **Memento**
   - Система сохранений и отката

---

## Форматы данных

### Система локализации

**Приоритеты источников:**

```
Приключения (priority 200) ← Наивысший приоритет
    ↓
Моды (priority 100) ← Средний приоритет  
    ↓
Базовая локализация (priority 0) ← Базовый уровень
```

**Принцип DRY реализован через:**
- Единую систему для всех источников (база, моды, приключения)
- Цепочку ответственности (Chain of Responsibility)
- Автоматическое переопределение (приоритеты)
- Централизованное кэширование

**Структура локализации:**

```yaml
# Базовая (data/yaml/localization.yaml)
ru:
  menu:
    continue: "Продолжить"
  combat:
    hit: "Вы нанесли {damage} урона {target}"

# Мод (data/mods/my_mod/localization.yaml)
ru:
  mod:
    my_mod:
      name: "Название мода"
  items:
    new_item:
      name: "Новый предмет"

# Приключение (data/adventures/quest/localization.yaml)
ru:
  adventure:
    quest:
      name: "Название приключения"
  locations:
    tavern:
      name: "Таверна"
      description: "Уютная таверна"
  dialogues:
    npc_greeting:
      text: "Приветствие NPC"
      option1: "Вариант ответа 1"
```

### YAML - Конфигурация игры

**data/yaml/settings.yaml:**
```yaml
game:
  title: "Dungeons and Dragons 5e"
  version: "0.1.0"
  min_terminal_size:
    width: 80
    height: 24

ui:
  theme: "default"
  show_borders: true
  use_colors: true
  language: "ru"

gameplay:
  autosave_enabled: true
  autosave_interval: 300  # секунды
  max_autosaves: 10
  difficulty: "normal"
```

**data/yaml/localization.yaml:**
```yaml
ru:
  menu:
    continue: "Продолжить"
    new_game: "Новая игра"
    load_game: "Загрузить"
    settings: "Настройки"
    exit: "Выход"
  
  messages:
    welcome: "Добро пожаловать в DnD!"
    save_success: "Игра успешно сохранена"
    load_success: "Игра успешно загружена"

en:
  menu:
    continue: "Continue"
    new_game: "New Game"
    load_game: "Load Game"
    settings: "Settings"
    exit: "Exit"
```

### JSON - Сохранения игры

**data/saves/autosave_01.json:**
```json
{
  "metadata": {
    "version": "0.1.0",
    "timestamp": "2024-01-15T10:30:00Z",
    "save_type": "autosave",
    "playtime": 3600
  },
  "character": {
    "name": "Aragorn",
    "race": "Human",
    "class": "Fighter",
    "level": 5,
    "experience": 6500,
    "stats": {
      "strength": 18,
      "dexterity": 14,
      "constitution": 16,
      "intelligence": 10,
      "wisdom": 12,
      "charisma": 13
    },
    "hp": {
      "current": 45,
      "max": 52,
      "temporary": 0
    },
    "resources": {
      "hit_dice": {
        "d10": 3
      },
      "second_wind": 1
    }
  },
  "inventory": {
    "gold": 250,
    "items": [
      {
        "id": "longsword",
        "quantity": 1,
        "equipped": true
      }
    ]
  },
  "location": {
    "area": "tavern",
    "room": "main_hall"
  },
  "quests": {
    "active": ["main_quest_01"],
    "completed": []
  },
  "game_time": {
    "day": 15,
    "hour": 14,
    "minute": 30
  }
}
```

---

## API документация

### Основные классы и методы

#### Game

```python
class Game:
    def __init__(self) -> None:
        """Инициализация игры"""
    
    def start(self) -> None:
        """Запуск игры"""
    
    def main_loop(self) -> None:
        """Главный игровой цикл"""
    
    def shutdown(self) -> None:
        """Корректное завершение игры"""
```

#### StateManager

```python
class StateManager:
    def set_state(self, state: GameState) -> None:
        """Установка нового состояния"""
    
    def get_state(self) -> GameState:
        """Получение текущего состояния"""
    
    def can_continue(self) -> bool:
        """Проверка наличия активной игры"""
```

#### Character

```python
class Character:
    def __init__(self, name: str, race: str, char_class: str) -> None:
        """Создание персонажа"""
    
    def get_modifier(self, stat: str) -> int:
        """Получение модификатора характеристики"""
    
    def take_damage(self, damage: int) -> None:
        """Получение урона"""
    
    def heal(self, amount: int) -> None:
        """Лечение"""
    
    def level_up(self) -> None:
        """Повышение уровня"""
```

---

## Требования к производительности

### Минимальные требования
- Python 3.13+
- 100 MB свободной RAM
- 50 MB свободного места на диске

### Ограничения
- Размер сохранения: < 1 MB
- Время загрузки игры: < 2 секунды
- FPS отрисовки UI: 30+ (при необходимости анимаций)

---

## Безопасность и надёжность

### Обработка ошибок
- Все операции с файлами обёрнуты в try-except
- Валидация пользовательского ввода
- Graceful degradation при ошибках

### Сохранность данных
- Атомарное сохранение (сначала во временный файл)
- Backup предыдущего сохранения
- Проверка целостности при загрузке

---

## Совместимость

### Платформы
- ✅ Windows 10/11
- ✅ Linux (Ubuntu 20.04+)
- ⚠️ macOS (не тестировалось)

### Терминалы
- ✅ Windows Terminal
- ✅ PowerShell
- ✅ CMD
- ✅ Linux Terminal (gnome-terminal, konsole, etc.)
- ✅ VS Code Terminal

---

## Сборка и развёртывание

### cx_Freeze конфигурация

Конфигурация находится в `pyproject.toml`:

```toml
[tool.cx_Freeze]
excludes = ["tkinter", "unittest", "matplotlib", "numpy"]
optimize = 1
```

Дополнительная конфигурация в `setup.py`.

### Команды сборки

```bash
# Установить dev-зависимости
pip install -e ".[dev]"

# Создать сборку
python setup.py build

# Исполняемые файлы будут в build/exe.*/
```

---

## Тестирование

### Покрытие тестами
- Минимальное покрытие: 80%
- Критические системы: 100%

### Типы тестов
- Unit tests - тестирование отдельных компонентов
- Integration tests - тестирование взаимодействия систем
- E2E tests - тестирование полных сценариев

---

## Changelog

### v0.1.0 (В разработке)
- Инициализация проекта
- Структура проекта
- Базовая документация

---

*Документация обновляется по мере развития проекта*