# Интернационализация (i18n) в D&D MUD

## Обзор

Проект поддерживает интернационализацию с использованием стандартной библиотеки `gettext` и паттерна **Singleton** для управления локализацией.

## Структура файлов

```
dnd_mud/
├── locales/
│   └── ru/
│       └── LC_MESSAGES/
│           ├── dnd_mud.po    # Файл переводов (текстовый)
│           └── dnd_mud.mo    # Скомпилированный файл (бинарный)
├── src/
│   └── i18n.py               # Менеджер локализации
├── babel.cfg                 # Конфигурация Babel
├── compile_mo.py             # Скрипт компиляции .po → .mo
└── setup.py                  # Setup script с компиляцией
```

## Использование

### 1. Добавление функции перевода

```python
from src.i18n import _

# Простая строка
print(_('Привет, мир!'))

# Строка с форматированием
name = "Игрок"
print(f'_{Привет, {name}!')')  # Неверно!
print(_('Привет, {}!').format(name))  # Верно
```

### 2. Изменение языка

```python
from src.i18n import set_language, get_current_language

# Установить язык
set_language('en')  # Английский
set_language('ru')  # Русский (по умолчанию)

# Получить текущий язык
current_lang = get_current_language()
print(f'Текущий язык: {current_lang}')
```

### 3. Получение доступных языков

```python
from src.i18n import get_available_languages

languages = get_available_languages()
print(f'Доступные языки: {languages}')
```

## Архитектура

### Паттерн Singleton

Класс `I18nManager` реализует паттерн **Singleton** для обеспечения единственного экземпляра менеджера локализации:

```python
class I18nManager:
    _instance: Optional['I18nManager'] = None
    
    def __new__(cls) -> 'I18nManager':
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```

**Преимущества:**
- Единая точка доступа к переводам
- Экономия памяти
- Глобальная конфигурация языка

### Принципы SOLID

- **Single Responsibility**: `I18nManager` отвечает только за локализацию
- **Open/Closed**: Можно добавлять новые языки без изменения кода
- **Dependency Inversion**: Зависимости от абстракций (`gettext`)

## Добавление нового языка

### 1. Создание директории

```bash
mkdir -p locales/en/LC_MESSAGES
```

### 2. Копирование и перевод .po файла

```bash
cp locales/ru/LC_MESSAGES/dnd_mud.po locales/en/LC_MESSAGES/
```

Отредактируйте `locales/en/LC_MESSAGES/dnd_mud.po`:

```po
msgid ""
msgstr ""
"Language: en\n"
"Content-Type: text/plain; charset=UTF-8\n"

msgid "Новая игра"
msgstr "New Game"

msgid "Загрузить игру"
msgstr "Load Game"
```

### 3. Компиляция

```bash
python3 compile_mo.py
```

## Обновление переводов

При добавлении новых строк в код:

### 1. Извлечение строк

```bash
pybabel extract -F babel.cfg -o locales/dnd_mud.pot .
```

### 2. Обновление .po файлов

```bash
pybabel update -i locales/dnd_mud.pot -d locales/
```

### 3. Компиляция

```bash
python3 compile_mo.py
```

## Рекомендации

### ✅ Правильно

```python
# Простые строки
print(_('Меню'))

# Строки с параметрами
name = 'Игрок'
print(_('Привет, {}!').format(name))

# Многострочные строки
message = _(
    "Это многострочное сообщение\n"
    "с переносами строк"
)
```

### ❌ Неправильно

```python
# Вложенные вызовы функции
print(_('Привет, {}!').format(_('Игрок')))

# Конкатенация переведенных строк
print(_('Привет, ') + _('мир!'))

# Использование f-строк с переводом
name = 'Игрок'
print(f'_{Привет, {name}!')')  # Не работает!
```

### 🎯 Лучшие практики

1. **Извлекайте все пользовательские строки** - весь текст, который видит пользователь
2. **Используйте осмысленные msgid** - они должны быть понятны переводчикам
3. **Группируйте связанные строки** - для лучшего контекста
4. **Тестируйте разные языки** - проверяйте отображение интерфейса
5. **Документируйте контекст** - добавляйте комментарии в .po файлы при необходимости

## Устранение проблем

### Проблема: Переводы не работают

**Решение:**
1. Проверьте существование .mo файла: `locales/ru/LC_MESSAGES/dnd_mud.mo`
2. Перекомпилируйте: `python3 compile_mo.py`
3. Проверьте права доступа к файлам

### Проблема: Неправильное отображение символов

**Решение:**
1. Убедитесь что .po файл в UTF-8 кодировке
2. Проверьте заголовок .po файла: `charset=UTF-8`
3. Используйте правильную системную локаль

### Проблема: Singleton не работает

**Решение:**
1. Убедитесь что импорт правильный: `from src.i18n import _`
2. Проверьте что используется глобальная функция, а не класс напрямую

## Зависимости

- `Babel>=2.14.0` - для инструментов извлечения и обновления переводов
- `gettext` - стандартная библиотека Python для локализации

## Автоматизация

### Setup.py

Скрипт `setup.py` автоматически компилирует .mo файлы при установке:

```bash
python3 setup.py install
```

### Makefile (опционально)

Можно добавить Makefile для удобства:

```makefile
compile-mo:
	python3 compile_mo.py

extract-messages:
	pybabel extract -F babel.cfg -o locales/dnd_mud.pot .

update-messages:
	pybabel update -i locales/dnd_mud.pot -d locales/

.PHONY: compile-mo extract-messages update-messages
```
