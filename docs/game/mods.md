# 🔧 Руководство по созданию модов для DnD MUD Game

## Оглавление
1. [Введение](#введение)
2. [Быстрый старт](#быстрый-старт)
3. [Структура мода](#структура-мода)
4. [Обязательные файлы](#обязательные-файлы)
5. [Локализация](#локализация)
6. [Типы модов](#типы-модов)
7. [API и хуки](#api-и-хуки)
8. [Лучшие практики](#лучшие-практики)
9. [Примеры](#примеры)
10. [Публикация мода](#публикация-мода)

---

## Введение

Система модификаций DnD MUD Game позволяет расширять игру, добавляя:
- 🗡️ Новые предметы и снаряжение
- ⚔️ Новые механики боя
- 🔮 Новые заклинания
- 🎭 Новые классы и расы
- 🎲 Изменения игровых правил
- 🌍 Новые локации и события

**Ключевые принципы:**
- ✅ Каждый мод ОБЯЗАН иметь `localization.yaml`
- ✅ Моды не должны конфликтовать с базовой игрой
- ✅ Следование принципам SOLID и DRY
- ✅ Тщательное тестирование

---

## Быстрый старт

### Создание первого мода за 5 минут

```bash
# 1. Создать структуру
cd data/mods
mkdir my_first_mod
cd my_first_mod

# 2. Создать mod.yaml
cat > mod.yaml << 'EOF'
name: "My First Mod"
name_localized: "mod.my_first_mod.name"
version: "1.0.0"
author: "Your Name"
description_localized: "mod.my_first_mod.description"
compatible_version: "0.1.0"

adds:
  items: true
  mechanics: false
  spells: false
  classes: false
  races: false

modifies:
  combat_system: false
  damage_types: false
  conditions: false
EOF

# 3. Создать localization.yaml (ОБЯЗАТЕЛЬНО!)
cat > localization.yaml << 'EOF'
ru:
  mod:
    my_first_mod:
      name: "Мой Первый Мод"
      description: "Добавляет несколько новых предметов"
      
  items:
    magic_staff:
      name: "Волшебный посох"
      description: "Простой посох с магическими свойствами"
      effect: "+1 к броскам атаки заклинаниями"

en:
  mod:
    my_first_mod:
      name: "My First Mod"
      description: "Adds several new items"
      
  items:
    magic_staff:
      name: "Magic Staff"
      description: "Simple staff with magical properties"
      effect: "+1 to spell attack rolls"
EOF

# 4. Создать items.yaml
cat > items.yaml << 'EOF'
items:
  magic_staff:
    id: "magic_staff"
    type: "weapon"
    category: "staff"
    rarity: "uncommon"
    cost: 500
    weight: 4
    damage: "1d6"
    damage_type: "bludgeoning"
    properties:
      - "versatile"
    magic_bonus: 1
    requires_attunement: false
EOF

# 5. Готово! Мод создан
```

Запустите игру - мод загрузится автоматически.

---

## Структура мода

### Минимальная структура

```
data/mods/my_mod/
├── mod.yaml              # Метаданные (ОБЯЗАТЕЛЬНО)
├── localization.yaml     # Локализация (ОБЯЗАТЕЛЬНО!)
└── README.md            # Описание (рекомендуется)
```

### Полная структура

```
data/mods/my_mod/
├── mod.yaml              # Метаданные
├── localization.yaml     # Локализация
├── README.md            # Документация
│
├── items/               # Новые предметы
│   ├── weapons.yaml
│   ├── armor.yaml
│   └── consumables.yaml
│
├── spells/              # Новые заклинания
│   ├── cantrips.yaml
│   └── leveled_spells.yaml
│
├── classes/             # Новые классы
│   └── my_class.yaml
│
├── races/               # Новые расы
│   └── my_race.yaml
│
├── mechanics/           # Изменения механик
│   ├── combat.yaml
│   └── magic.yaml
│
├── scripts/             # Python скрипты (расширенные моды)
│   ├── __init__.py
│   └── hooks.py
│
└── assets/              # Ресурсы (если нужны)
    └── images/
```

---

## Обязательные файлы

### 1. mod.yaml

Метаданные мода - обязательный файл.

```yaml
# Основная информация
name: "Advanced Combat Mod"
name_localized: "mod.advanced_combat.name"
version: "1.2.0"
author: "Your Name"
author_contact: "email@example.com"
description_localized: "mod.advanced_combat.description"
compatible_version: "0.1.0"

# Зависимости (опционально)
requires:
  mods: []  # Список ID других модов
  min_game_version: "0.1.0"

# Что добавляет мод
adds:
  items: true           # Добавляет новые предметы
  spells: true         # Добавляет заклинания
  mechanics: true      # Добавляет новые механики
  classes: false       # НЕ добавляет классы
  races: false         # НЕ добавляет расы
  locations: false     # НЕ добавляет локации

# Что изменяет
modifies:
  combat_system: true   # Изменяет боевую систему
  damage_types: true    # Добавляет типы урона
  conditions: true      # Добавляет состояния
  resting: false       # НЕ изменяет отдых
  leveling: false      # НЕ изменяет прокачку

# Настройки совместимости
compatibility:
  conflicts_with: []    # Список несовместимых модов
  load_after: []        # Загружать после этих модов
  load_before: []       # Загружать до этих модов

# Метаданные
metadata:
  homepage: "https://github.com/yourname/mod"
  repository: "https://github.com/yourname/mod"
  license: "MIT"
  tags: ["combat", "mechanics", "advanced"]
```

### 2. localization.yaml

**ОБЯЗАТЕЛЬНЫЙ** файл с переводами.

```yaml
ru:
  # Метаданные мода
  mod:
    mod_id:
      name: "Название мода"
      description: "Описание мода"
      
  # Секции мода
  items:
    item_id:
      name: "Название предмета"
      description: "Описание предмета"
      
  spells:
    spell_id:
      name: "Название заклинания"
      description: "Описание заклинания"
      
  mechanics:
    mechanic_id:
      name: "Название механики"
      description: "Описание механики"
      
  # Сообщения
  messages:
    event_happened: "Произошло событие {event}"

en:
  mod:
    mod_id:
      name: "Mod Name"
      description: "Mod Description"
      
  # ... английские переводы
```

**Требования к локализации:**
- ✅ Минимум русский язык (`ru`)
- ✅ Все текстовые строки через локализацию
- ✅ Использование `{параметров}` для форматирования
- ✅ Понятные ключи в формате `section.subsection.key`

### 3. README.md (рекомендуется)

```markdown
# Название Мода

Краткое описание что делает мод.

## Возможности

- ✅ Функция 1
- ✅ Функция 2
- ✅ Функция 3

## Установка

1. Скачать мод
2. Распаковать в `data/mods/`
3. Запустить игру

## Настройка

Опциональные настройки через игровое меню.

## Совместимость

- ✅ Совместим с базовой игрой v0.1.0+
- ⚠️ Несовместим с модом X
- ✅ Работает с модом Y

## Лицензия

MIT License
```

---

## Локализация

### Структура ключей

Используйте иерархическую структуру:

```yaml
ru:
  mod:
    mod_id:
      name: "..."
      description: "..."
      
  category:
    subcategory:
      item_id:
        property: "значение"
```

### Примеры локализации

**Предметы:**
```yaml
items:
  longsword_plus_one:
    name: "Длинный меч +1"
    description: "Отлично сбалансированный длинный меч"
    effect: "+1 к броскам атаки и урона"
```

**Заклинания:**
```yaml
spells:
  fireball:
    name: "Огненный шар"
    description: "Яркий шар пламени вылетает из вашей руки"
    effect: "8d6 огненного урона"
    higher_levels: "Урон увеличивается на 1d6 за каждый уровень выше 3-го"
```

**Механики:**
```yaml
mechanics:
  flanking:
    name: "Фланкирование"
    description: "Когда вы и союзник атакуете врага с противоположных сторон"
    effect: "Вы получаете преимущество на броски атаки"
```

**Сообщения с параметрами:**
```yaml
messages:
  item_found: "Вы нашли {item_name}!"
  damage_dealt: "{attacker} нанёс {damage} урона {target}"
  spell_cast: "{caster} использует {spell_name}"
```

### Использование в коде

```python
from src.data.localization import loc

# Простое получение
item_name = loc("items.magic_sword.name")
# "Волшебный меч"

# С параметрами
message = loc("messages.damage_dealt",
             attacker="Воин",
             damage=15,
             target="Гоблин")
# "Воин нанёс 15 урона Гоблин"
```

---

## Типы модов

### 1. Контентные моды

Добавляют новый контент без изменения механик.

**Пример: New Items Pack**
```yaml
adds:
  items: true
  spells: false
  mechanics: false

modifies:
  combat_system: false
```

**Содержимое:**
- `items/` - новые предметы
- `localization.yaml` - переводы

### 2. Механические моды

Изменяют или добавляют игровые механики.

**Пример: Advanced Combat**
```yaml
adds:
  mechanics: true

modifies:
  combat_system: true
  damage_types: true
```

**Содержимое:**
- `mechanics/combat.yaml` - изменения боя
- `localization.yaml` - переводы

### 3. Балансные моды

Изменяют баланс игры.

**Пример: Hardcore Mode**
```yaml
modifies:
  combat_system: true
  resting: true
  leveling: true
```

### 4. Полные моды (Total Conversion)

Значительно изменяют игру.

**Пример: Sci-Fi Conversion**
```yaml
adds:
  items: true
  classes: true
  races: true
  locations: true

modifies:
  combat_system: true
  magic_system: true
```

---

## API и хуки

### Хуки событий

Моды могут подписываться на события игры:

```python
# scripts/hooks.py

from src.systems.mod_api import ModHook

class MyModHooks(ModHook):
    """Хуки для мода."""
    
    def on_combat_start(self, combat_data):
        """Вызывается при начале боя."""
        print("Бой начался!")
        # Ваша логика
        
    def on_damage_dealt(self, attacker, target, damage):
        """Вызывается при нанесении урона."""
        # Можно изменить урон
        if attacker.has_buff("rage"):
            damage *= 1.5
        return damage
    
    def on_item_use(self, character, item):
        """Вызывается при использовании предмета."""
        pass
    
    def on_spell_cast(self, caster, spell, target):
        """Вызывается при касте заклинания."""
        pass
    
    def on_level_up(self, character, new_level):
        """Вызывается при повышении уровня."""
        pass
```

### Доступные хуки

| Хук | Описание | Параметры |
|-----|----------|-----------|
| `on_game_start` | Начало игры | - |
| `on_combat_start` | Начало боя | `combat_data` |
| `on_combat_end` | Конец боя | `combat_data`, `result` |
| `on_turn_start` | Начало хода | `character` |
| `on_damage_dealt` | Нанесение урона | `attacker`, `target`, `damage` |
| `on_healing` | Лечение | `healer`, `target`, `amount` |
| `on_item_use` | Использование предмета | `character`, `item` |
| `on_spell_cast` | Каст заклинания | `caster`, `spell`, `target` |
| `on_level_up` | Повышение уровня | `character`, `new_level` |
| `on_rest_start` | Начало отдыха | `character`, `rest_type` |
| `on_rest_end` | Конец отдыха | `character`, `rest_type` |

---

## Лучшие практики

### ✅ DO (Делайте)

1. **Всегда создавайте localization.yaml**
   ```yaml
   # ✅ Хорошо - есть переводы
   localization.yaml
   ```

2. **Используйте понятные ID**
   ```yaml
   # ✅ Хорошо
   items:
     longsword_plus_one:
       id: "longsword_plus_one"
   
   # ❌ Плохо
   items:
     item_001:
       id: "ls_p1"
   ```

3. **Тестируйте совместимость**
   - Тестируйте с базовой игрой
   - Тестируйте с популярными модами
   - Указывайте конфликты в `mod.yaml`

4. **Документируйте изменения**
   ```markdown
   ## Changelog
   
   ### v1.2.0
   - Добавлено 5 новых предметов
   - Исправлен баг с уроном
   ```

5. **Следуйте версионированию**
   - `MAJOR.MINOR.PATCH`
   - `1.0.0` - первый релиз
   - `1.1.0` - новые функции
   - `1.0.1` - багфиксы

### ❌ DON'T (Не делайте)

1. **Не создавайте моды без локализации**
   ```
   # ❌ Плохо
   my_mod/
     mod.yaml
     # localization.yaml отсутствует!
   ```

2. **Не хардкодите строки**
   ```python
   # ❌ Плохо
   print("Вы нашли меч!")
   
   # ✅ Хорошо
   print(loc("messages.item_found", item="меч"))
   ```

3. **Не изменяйте базовые файлы игры**
   - Всё в папке мода
   - Не трогайте `data/yaml/` базовой игры

4. **Не конфликтуйте с другими модами**
   - Используйте уникальные ID
   - Указывайте конфликты

5. **Не забывайте о производительности**
   - Оптимизируйте хуки
   - Не создавайте слишком сложную логику

---

## Примеры

### Пример 1: Простой мод с предметами

**Структура:**
```
data/mods/simple_items/
├── mod.yaml
├── localization.yaml
└── items.yaml
```

**mod.yaml:**
```yaml
name: "Simple Items Mod"
name_localized: "mod.simple_items.name"
version: "1.0.0"
author: "John Doe"
description_localized: "mod.simple_items.description"
compatible_version: "0.1.0"

adds:
  items: true
```

**localization.yaml:**
```yaml
ru:
  mod:
    simple_items:
      name: "Простые Предметы"
      description: "Добавляет 10 новых предметов"
      
  items:
    health_potion_greater:
      name: "Большое зелье лечения"
      description: "Восстанавливает 4d4+4 хитов"
```

**items.yaml:**
```yaml
items:
  health_potion_greater:
    id: "health_potion_greater"
    type: "consumable"
    category: "potion"
    rarity: "uncommon"
    cost: 100
    weight: 0.5
    healing: "4d4+4"
```

### Пример 2: Мод с механиками

**Структура:**
```
data/mods/flanking_rules/
├── mod.yaml
├── localization.yaml
└── mechanics/
    └── combat.yaml
```

**mechanics/combat.yaml:**
```yaml
combat_rules:
  flanking:
    enabled: true
    advantage_type: "full"
    minimum_allies: 1
    opposite_sides_required: true
```

---

## Публикация мода

### 1. Подготовка к релизу

**Чеклист:**
- [ ] Все файлы на месте
- [ ] `localization.yaml` полный (ru + en)
- [ ] `README.md` написан
- [ ] Версия в `mod.yaml` корректна
- [ ] Тестирование завершено
- [ ] Скриншоты подготовлены (если нужно)

### 2. Упаковка

```bash
# Создать архив
cd data/mods
zip -r my_mod_v1.0.0.zip my_mod/

# Или tar.gz
tar -czf my_mod_v1.0.0.tar.gz my_mod/
```

### 3. Публикация

**GitHub:**
1. Создать репозиторий
2. Загрузить мод
3. Создать Release с архивом
4. Добавить описание и скриншоты

**Форум/Discord:**
1. Создать тему
2. Описать возможности
3. Прикрепить архив
4. Указать системные требования

### 4. Структура Release

```
my_mod_v1.0.0.zip
└── my_mod/
    ├── mod.yaml
    ├── localization.yaml
    ├── README.md
    ├── CHANGELOG.md
    └── ... остальные файлы
```

---

## FAQ

**Q: Обязательна ли локализация?**  
A: Да! Каждый мод ОБЯЗАН иметь `localization.yaml` с минимум русским языком.

**Q: Можно ли изменять базовые механики?**  
A: Да, через систему хуков и `mechanics/` файлы.

**Q: Как узнать ID предметов базовой игры?**  
A: Смотрите `data/yaml/game_config.yaml` базовой игры.

**Q: Что если мой мод конфликтует с другим?**  
A: Укажите это в `mod.yaml` в секции `compatibility.conflicts_with`.

**Q: Нужно ли указывать лицензию?**  
A: Рекомендуется. По умолчанию подразумевается MIT.

**Q: Можно ли монетизировать моды?**  
A: Следуйте лицензии базовой игры. Обычно нельзя продавать, но можно принимать донаты.

---

## Поддержка и сообщество

### Получить помощь

- 📖 [Документация](../README.md)
- 💬 [Discord сервер](https://discord.gg/...)
- 🐛 [Баг-трекер](https://github.com/.../issues)
- 📧 Email: support@example.com

### Полезные ресурсы

- [Примеры модов](https://github.com/.../example-mods)
- [API документация](API.md)
- [Система локализации](LOCALIZATION.md)
- [Видео-туториалы](https://youtube.com/...)

---

## Шаблон мода

Используйте этот шаблон для быстрого старта:

```bash
# Скачать шаблон
git clone https://github.com/.../mod-template.git my_new_mod
cd my_new_mod

# Заполнить информацию
nano mod.yaml
nano localization.yaml

# Начать разработку!
```

---

**Удачи в создании модов! 🎮**

*Если у вас есть вопросы или предложения по улучшению этого руководства, пожалуйста, создайте Issue на GitHub.*