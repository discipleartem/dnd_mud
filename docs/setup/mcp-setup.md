# Настройка MCP и мульти-агентности

Полное руководство по настройке MCP серверов, cursor-agent и мульти-агентной системы для проекта DnD MUD Game.

## 📋 Содержание

1. [Установка зависимостей](#установка-зависимостей)
2. [Настройка MCP серверов](#настройка-mcp-серверов)
3. [Аутентификация cursor-agent](#аутентификация-cursor-agent)
4. [Проверка конфигурации](#проверка-конфигурации)
5. [Устранение проблем](#устранение-проблем)

## Установка зависимостей

### Node.js и npx

```bash
# Установка через nvm (рекомендуется)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20

# Проверка
node --version  # Должно быть v20.x.x
npm --version
npx --version
```

### cursor-agent CLI

```bash
# Установка через официальный установщик
curl https://cursor.com/install -fsS | bash

# Добавьте в PATH (если не добавлено автоматически)
export PATH="$HOME/.local/bin:$PATH"
# Или добавьте в ~/.bashrc:
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# Проверка установки
cursor-agent --version
```

## Настройка MCP серверов

### Конфигурация `~/.cursor/mcp.json`

Создайте или обновите файл `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "sub-agents": {
      "command": "npx",
      "args": ["-y", "sub-agents-mcp"],
      "env": {
        "AGENTS_DIR": "/home/tomas/WORK/dnd_mud/docs/agents",
        "AGENT_TYPE": "cursor",
        "EXECUTION_TIMEOUT_MS": "600000",
        "SESSION_ENABLED": "true",
        "SESSION_DIR": "/home/tomas/WORK/dnd_mud/docs/agents/sessions",
        "SESSION_RETENTION_DAYS": "1",
        "DEFAULT_MODEL": "claude-sonnet-4.5",
        "DEBUG_CURSOR_MCP": "true",
        "CURSOR_AGENT_IDLE_EXIT_MS": "0",
        "LOG_LEVEL": "debug",
        "PATH": "/home/tomas/.local/bin:/home/tomas/.cargo/bin:/opt/cursor/bin:/usr/local/bin:/usr/bin:/bin:/usr/local/sbin:/usr/sbin:/sbin"
      }
    }
  }
}
```

**Важно:** Замените `/home/tomas/WORK/dnd_mud` на абсолютный путь к вашему проекту.

### Разрешения для агентов

Создайте файл `.cursor/cli.json` в корне проекта:

```json
{
  "permissions": {
    "allow": [
      "Shell(cd)",
      "Shell(ls)",
      "Shell(cat)",
      "Shell(git)",
      "Shell(npm)",
      "Shell(npx)",
      "Shell(python)",
      "Shell(pytest)",
      "Shell(black)",
      "Shell(ruff)",
      "Shell(mypy)"
    ],
    "deny": [
      "Shell(rm -rf)",
      "Shell(sudo)",
      "Shell(chmod)",
      "Shell(chown)"
    ]
  }
}
```

## Аутентификация cursor-agent

### Способ 1: Интерактивный login (рекомендуется)

```bash
export PATH="$HOME/.local/bin:$PATH"
cursor-agent login
```

Команда откроет браузер для аутентификации. После успешной аутентификации проверьте статус:

```bash
cursor-agent status
```

### Способ 2: Использование API ключа

1. Получите API ключ из панели управления Cursor: https://cursor.com/settings
2. Добавьте в `~/.cursor/mcp.json` в секцию `env`:

```json
{
  "mcpServers": {
    "sub-agents": {
      "env": {
        "CURSOR_API_KEY": "ваш_api_ключ_здесь",
        ...
      }
    }
  }
}
```

3. Перезапустите Cursor IDE для применения изменений

## Проверка конфигурации

Запустите скрипт проверки:

```bash
./scripts/test_mcp_config.sh
```

Скрипт проверит:
- Валидность JSON конфигураций
- Наличие и корректность определений агентов
- Существование необходимых директорий
- Установку зависимостей (Node.js, npx, cursor-agent)
- Настройку разрешений

## Устранение проблем

### Агенты не запускаются

1. **Проверьте логи MCP сервера:**
   - Help → Toggle Developer Tools
   - Проверьте консоль на ошибки

2. **Проверьте аутентификацию:**
   ```bash
   cursor-agent status
   ```

3. **Проверьте установку зависимостей:**
   ```bash
   node --version
   npx --version
   cursor-agent --version
   ```

4. **Проверьте конфигурацию:**
   ```bash
   ./scripts/test_mcp_config.sh
   ```

### Ошибка: "Agent not found"

- Проверьте, что файл агента существует в `docs/agents/`
- Проверьте имя агента (должно совпадать с именем файла без расширения)

### Ошибка: "Authentication required"

- Выполните `cursor-agent login`
- Или добавьте `CURSOR_API_KEY` в `~/.cursor/mcp.json`

## 📋 Следующие шаги

После настройки:

1. Перезапустите Cursor IDE для применения изменений
2. Протестируйте запуск агента через MCP
3. Ознакомьтесь с [автоматическим роутингом](../mcp/auto-routing.md)
4. Изучите [определения агентов](../agents/README.md)

## 📚 Дополнительная документация

- [Аутентификация cursor-agent](./authentication.md) - подробное руководство
- [Быстрый старт](./quick-start.md) - краткое руководство
- [Документация по MCP](../mcp/README.md) - работа с мульти-агентностью
- [Тестирование MCP](../mcp/testing.md) - тестирование работоспособности
