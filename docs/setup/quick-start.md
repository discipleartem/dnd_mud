# Быстрый старт: Аутентификация и запуск агентов

## ⚡ Быстрая аутентификация

Выполните одну из команд:

### Вариант 1: Автоматический скрипт

```bash
cd /home/tomas/WORK/dnd_mud
./scripts/authenticate_cursor_agent.sh
```

Скрипт проверит статус и предложит варианты аутентификации.

### Вариант 2: Ручная команда

```bash
export PATH="$HOME/.local/bin:$PATH"
cursor-agent login
```

Команда откроет браузер для аутентификации.

## ✅ После аутентификации

1. **Проверьте статус:**
   ```bash
   cursor-agent status
   ```

2. **Перезапустите Cursor IDE** (чтобы MCP сервер подхватил изменения)

3. **Протестируйте запуск агента:**
   ```python
   # В Cursor IDE через MCP
   mcp_sub-agents_run_agent(
       agent="code-reviewer",
       prompt="Проверь код файла /home/tomas/WORK/dnd_mud/src/core/window_manager.py"
   )
   ```

## 🎯 Готовые команды для копирования

```bash
# 1. Аутентификация
export PATH="$HOME/.local/bin:$PATH"
cursor-agent login

# 2. Проверка статуса
cursor-agent status

# 3. После успешной аутентификации - перезапустите Cursor IDE
```

## 📚 Подробная документация

- `docs/AUTHENTICATION_GUIDE.md` - полное руководство по аутентификации
- `docs/CURSOR_AGENT_SETUP.md` - настройка cursor-agent
- `docs/MCP_CHECK_REPORT.md` - отчет о проверке MCP серверов

