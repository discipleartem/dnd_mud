# Универсальная система рас D&D MUD

## Обзор

Новая универсальная система рас создана для решения проблем с расширяемостью и удобством добавления нового контента. Она поддерживает модификации, унифицированный формат особенностей и обратную совместимость.

## 🎯 Решенные проблемы

### Старая структура:
- ❌ Жесткая структура бонусов
- ❌ Сложное добавление подрас
- ❌ Отсутствие поддержки модификаций
- ❌ Разнородный формат особенностей
- ❌ Сложное форматирование для отображения

### Новая структура:
- ✅ Универсальная система особенностей
- ✅ Поддержка модификаций
- ✅ Наследование бонусов и особенностей
- ✅ Автоматическое форматирование
- ✅ Обратная совместимость

## 📁 Структура файлов

```
data/yaml/races/
├── races_new.yaml          # Новая универсальная структура
├── races.yaml             # Старая структура (для совместимости)
└── mods/                  # Модификации
    ├── mod_name/
    │   └── races_mod.yaml
    └── another_mod/
        └── races_mod.yaml
```

## 🏗️ Структура YAML

### Основные секции

```yaml
# Шаблоны для переиспользования
templates:
  ability_choice_template:
    type: "ability_choice"
    description_template: "Выберите {max_choices} характеристик (+{bonus_value} к каждой)"
    # ...

# Расы
races:
  race_key:
    name: "Название расы"
    description: "Подробное описание"
    bonuses: { }              # Базовые бонусы
    features: [ ]             # Общие особенности
    subraces:                # Подрасы
      subrace_key:
        name: "Название подрасы"
        description: "Описание"
        bonuses: { }          # Бонусы подрасы
        features: [ ]         # Особенности подрасы
        inherit_bonuses: true/false
        inherit_features: true/false

# Модификации (в отдельных файлах)
modifications:
  mod_name:
    name: "Название модификации"
    affects_races: ["race1", "race2"]
    changes:
      race_key:
        bonuses: { }
        features: [ ]
        subraces: { }
```

## 🎲 Типы особенностей

### 1. `ability_choice` - Выбор характеристик
```yaml
- type: "ability_choice"
  name: "Увеличение характеристик"
  description: "Выберите 2 любые характеристики (+1 к каждой)"
  max_choices: 2
  bonus_value: 1
  allowed_attributes: ["strength", "dexterity", ...]
```

### 2. `skill_choice` - Выбор навыков
```yaml
- type: "skill_choice"
  name: "Владение навыком"
  description: "Выберите один навык для владения"
  max_choices: 1
```

### 3. `feat_choice` - Выбор черт
```yaml
- type: "feat_choice"
  name: "Черта"
  description: "Выберите одну черту на ваш выбор"
```

### 4. `trait` - Пассивные черты
```yaml
- type: "trait"
  name: "Яркий ум"
  description: "Получаете преимущество на спасброски от очарования"
  effect: "advantage_charm_save"
```

### 5. `proficiency` - Владения
```yaml
- type: "proficiency"
  name: "Владение оружием"
  description: "Владеете длинными мечами, луками и арбалетами"
  weapons: ["longsword", "longbow", "shortbow"]
```

### 6. `spell` - Заклинания
```yaml
- type: "spell"
  name: "Заклинание высокого эльфа"
  description: "Можете создавать одно заговор по выбору"
  spells: ["cantrip_choice"]
  ability: "intelligence"
```

### 7. `language` - Языки
```yaml
- type: "language"
  name: "Языки"
  description: "Можете говорить на Общем и Эльфийском"
  languages: ["common", "elvish"]
```

## 📋 Наследование

### Бонусы
- `inherit_bonuses: true` - Подраса наследует бонусы основной расы
- `inherit_bonuses: false` - Подраса использует только свои бонусы

### Особенности
- `inherit_features: true` - Подраса наследует особенности основной расы
- `inherit_features: false` - Подраса использует только свои особенности

## 🔧 Модификации

Модификации позволяют изменять расы без изменения основного файла:

### Структура файла модификации
```yaml
# data/mods/my_mod/races_mod.yaml
modifications:
  my_mod:
    name: "Моя модификация"
    description: "Описание модификации"
    affects_races: ["human", "elf"]
    
    changes:
      human:
        bonuses:
          strength: 1        # Добавить к существующим
        
        features:
          - type: "trait"
            name: "Новая черта"
            description: "Описание новой черты"
      
      elf:
        subraces:
          moon_elf:         # Добавить новую подрасу
            name: "Лунный эльф"
            description: "..."
            inherit_bonuses: true
            bonuses:
              wisdom: 1
```

## 🚀 Использование

### Базовое использование
```python
from domain.entities.universal_race_factory import UniversalRaceFactory

# Получить выбор рас
choices = UniversalRaceFactory.get_race_choices()

# Создать расу
race = UniversalRaceFactory.create_race("human", "variant")

# Получить отформатированную информацию
info = UniversalRaceFactory.get_formatted_race_info("elf", "high_elf")
```

### Интеграция в UI
```python
def _display_race_info(self, race_key: str, race_name: str, choice_num: int, subrace_key: str = None):
    info = UniversalRaceFactory.get_formatted_race_info(race_key, subrace_key)
    
    print(f"{choice_num}. {info['name']}")
    print(f"   📖 {info['description']}")
    print(f"   🎯 Бонусы: {info['bonuses']}")
    
    if info['features']:
        print(f"   ✨ Особенности:")
        print(info['features'])
```

## 🔄 Миграция со старой структуры

Система автоматически конвертирует старый формат:
- `alternative_features` → `features` с соответствующими типами
- Сохраняет совместимость с существующими файлами
- Пошаговая миграция возможна

## 🎯 Преимущества

### Для разработчиков:
- **Универсальность** - единый формат для всех особенностей
- **Расширяемость** - легко добавлять новые типы особенностей
- **Модульность** - поддержка модификаций без изменения основного кода
- **Автоматизация** - умное форматирование и наследование

### Для игроков:
- **Информативность** - подробное описание всех особенностей
- **Гибкость** - больше вариантов кастомизации
- **Понятность** - четкое отображение бонусов и возможностей

### Для контент-мейкеров:
- **Простота** - легко добавлять новые расы и подрасы
- **Модификации** - создавать контент без изменения оригинала
- **Шаблоны** - переиспользовать готовые решения

## 🔮 Будущие расширения

### Планируемые возможности:
1. **Динамические особенности** - особенности, меняющиеся от условий
2. **Синергии** - бонусы за комбинации рас/классов
3. **Кастомные шаблоны** - создаваемые пользователями шаблоны особенностей
4. **Визуальный редактор** - UI для создания модификаций
5. **Балансировщик** - автоматическая проверка баланса рас

## 📝 Пример добавления новой расы

```yaml
races:
  dragonborn:
    name: "Драконорожденный"
    description: "Потомок драконов, дышащий разрушительной энергией"
    bonuses:
      strength: 2
      charisma: 1
    
    features:
      - type: "trait"
        name: "Дыхание дракона"
        description: "Вы можете использовать дыхание дракона раз в короткий отдых"
        effect: "dragon_breath"
      
      - type: "damage_resistance"
        name: "Сопротивление урону"
        description: "Сопротивление урону вашего типа дракона"
        damage_types: ["fire", "cold", "lightning", "acid", "poison"]
    
    subraces:
      black_dragonborn:
        name: "Драконорожденный (черный)"
        description: "Потомок черных драконов"
        inherit_bonuses: true
        bonuses:
          dexterity: 1
        
        features:
          - type: "trait"
            name: "Кислотное дыхание"
            description: "Ваше дыхание наносит кислотный урон"
            effect: "acid_breath"
```

Эта система делает D&D MUD действительно расширяемой и удобной для модификаций! 🎲✨
