"""
Простое меню для D&D MUD.

Модуль предоставляет главное меню игры с использованием универсальной 
системы выбора из модуля user_choice.
"""

from .user_choice import get_user_choice

MAIN_MENU_ITEMS = ("Новая игра", "Загрузить игру", "Настройки","Выход")

# Стандартные сообщения
DEVELOPMENT_MESSAGE = "📝 В разработке."
CONTINUE_PROMPT = "Нажмите Enter для возврата..."


def show_menu() -> None:
    """Отображает главное меню с использованием универсальной системы выбора."""
    while True:
        choice = get_user_choice(
            items=MAIN_MENU_ITEMS,
            title="D&D MUD - ГЛАВНОЕ МЕНЮ"
        )
        
        if choice is None:
            continue  # Не должно произойти, так как allow_cancel=False
            
        selected_item = MAIN_MENU_ITEMS[choice - 1]  # Конвертируем в 0-based для доступа к списку
        
        # Обработка выбора основываясь на содержимом пункта меню
        if selected_item == "Выход":
            print("\n👋 До встречи!")
            import sys
            sys.exit(0)
        else:  # Остальные пункты
            print(f"\n📝 {selected_item} в разработке.")
            input(CONTINUE_PROMPT)